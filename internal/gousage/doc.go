// Package gousage finds packages in a Go module that import a target package.
//
// Use UsedBy to list the packages in the target package's module that import it; each result includes the using package's import path and filesystem location.
package gousage
