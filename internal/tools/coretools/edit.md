File editing tool that updates existing files via find-and-replace.
- `path` may be absolute or relative to the sandbox directory.
- Uses `old_text` and `new_text`; set `replace_all` to `true` to replace every match.
