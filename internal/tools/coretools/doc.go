// Package coretools provides basic file, shell, and planning tools for agents.
//
// The package includes tools for reading, listing, editing, writing, deleting, applying patches, running shell commands, and reporting plan updates. Tools that
// access the filesystem resolve relative paths against an authorizer's sandbox and use the authorizer for permission checks.
package coretools
