# PR

## User Summary (do not modify)

In this PR, we unify `codalotl cas ls-stale` and `codalotl cas ls-summary`.

Delete `codalotl cas ls-stale`.

Rename `codalotl cas ls-summary` to `codalotl cas ls-packages`.

Keep the same columns. But the names are confusing. "CAS" and "Prev CAS". I have no idea how someone would know what this means. Invent better terms to indicate "CAS entry is up to date" and "previously had a valid cas entry but it's stale now".

Add the ability to filter `ls-packages`'s results in various ways:
- only up-to-date packages
- only packags that are not up to date
- packages that never had a cas entry
- Only age > X
- Only churn > Y
- Some sensible way these combine.

Keep in mind the user. It's more important to add user value than it is to have a mathematical perfect system. Think about what the user is trying to do. Probably find packages that need to be refactored in some way, or don't have up-to-date status for soething. The age thing matters b/c users might want to apply a refactor at most  once a month for hotly changing packages.

This is your first phase. Design the command line arguments for ls-packages. Put the design as an artifact in this PR file (not in any SPEC.md yet). Once we get past this first phase, then you proceed with impl and SPEC.md changes and so on.
