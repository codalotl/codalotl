# PR

## User Summary (do not modify)


See .prs/2026-05-11_1778511306_clarify-cas.md and .prs/2026-05-11_1778530886_clarify-cas-improve.md: we made clarify_public_api spit out these CAS files for recorded Questions and Answers, and then a cli command `codalotl docs improve-from-clarify` which consumes them and updates docs.

We need to do 2 things now:
1. add this to the codalotl_cli tool.
2. tell orchestrator a bit about CAS and these doc improvements.
    - A VERY BRIEF AND CONCISE overview of what "CAS" is.
    - If it sees doc improvement cas entries, run the improve-from-clarity codalotl_cli, which should consume/delete them.
    - Otherwise, commit cas entries if it commits corresponding code.

Validation:
in addition to normal tests, make sure you manually try this with something like `go run . exec`. Construct a small fake repo and go project in a temp dir, and make a PR file, and run orchestrator. Make it run clarify for some reason, and then codalotl docs improve-from-clarify.

## Plan

### Phase 0: Verify codalotl_cli exposure [DONE]

#### Package internal/cli
- Confirmed the `codalotl_cli` whitelist exposes `codalotl docs improve-from-clarify`.
- Confirmed the limited command surface: docs commands are available, and non-tool docs commands such as `reflow` stay unavailable.

### Phase 1: Teach orchestrator about CAS/doc improvements [DONE]

#### Package internal/agentbuilder
- Added optional `codalotl_cli` to the `pr-orchestrator` agent tools.
- Updated the PR orchestrator prompt with a concise CAS overview.
- Told the orchestrator that clarify-public-api CAS records under `.codalotl/cas/clarify-public-api-1` should be consumed by running `codalotl docs improve-from-clarify` through `codalotl_cli`.
- Told the orchestrator to commit CAS records that correspond to committed code when they are not consumed by a tool workflow.

### Validation
- [DONE] Run focused tests for `internal/cli` and `internal/agentbuilder`.
- [DONE] Run normal project tests if practical.
- [DONE] Manually exercise with `go run . exec` in a temp fake repo/Go project: make an orchestration PR file, trigger clarify CAS records, then run/observe `codalotl docs improve-from-clarify` consumption.

## Review

- `review({"base":"origin/main"})`: attempted once; the review tool failed before returning findings because the full branch diff exceeded OpenAI's maximum tool input string length (`29659664` > `10485760`). No actionable review findings were returned.
- Pragmatic self-review: inspected this PR's focused diff against `origin/jn/improve-use`; no follow-up needed.
- `check_spec_conformance({"only_changed":true})`: `internal/agentbuilder` conforms. The generated SPEC conformance CAS record is committed with this review note.

## Summary

- Adds `codalotl_cli` to the `pr-orchestrator` agent so the orchestrator can run whitelisted codalotl commands.
- Adds concise CAS guidance to the PR orchestrator prompt:
  - CAS is content-addressed storage under `.codalotl/cas`.
  - clarify-public-api/doc-improvement CAS records should be consumed with `codalotl docs improve-from-clarify`.
  - other corresponding CAS records should be committed with the code they describe.
- Verifies the existing `codalotl_cli` command surface includes `codalotl docs improve-from-clarify` while keeping non-tool docs commands unavailable.

Validation:
- `go test ./internal/agentbuilder`
- `go test ./internal/cli`
- `go test ./...`
- Manual temp-repo orchestration run: triggered `clarify_public_api`, observed `.codalotl/cas/clarify-public-api-1`, ran `codalotl_cli` `docs improve-from-clarify`, and confirmed it processed/deleted 1 clarify record and applied 1 doc change.

## State

- Branch: `jn/improve-use`.
- Prior PRs created clarify-public-api CAS records and `codalotl docs improve-from-clarify`.
- `internal/cli/SPEC.md` already lists `codalotl_cli` support for `docs add` and `docs improve-from-clarify`.
- `internal/cli/codalotl_cli_tool_test.go` already checks the tool help includes `docs improve-from-clarify` and excludes non-tool docs commands such as `reflow`.
- Implementation commit `887150e` added `codalotl_cli` to `pr-orchestrator`, added prompt CAS/doc-improvement guidance, and updated focused agentbuilder tests.
- Focused tests passed: `go test ./internal/agentbuilder`; `go test ./internal/cli`.
- Normal tests passed: `go test ./...`.
- Manual validation used a temp fake repo/Go project and the current built binary via `codalotl exec --yes --json --slash-command=orchestrate`. The orchestrator called `clarify_public_api` for `internal/demo.Add`, then ran `codalotl_cli` `docs improve-from-clarify`; output reported `Processed 1 clarify record(s); deleted 1; applied 1 documentation change(s).`
