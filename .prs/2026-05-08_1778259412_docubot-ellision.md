# PR

## User Summary (do not modify)

When I run the refactor tool, I see something like this:

```
• Refactoring docs-add in internal/gocode
  • Need docs for 16 identifiers
  • > Requesting docs for 12 identifiers: FilterIdentifiersOptions, FilterIdentifiersOptionsAll, FilterIdentifiersOptionsAllNonGenerated,
    FilterIdentifiersOptionsDocumentedNonAmbiguous, FilterIdentifiersOptionsNonAmbiguous, ErrResolveNotFound, UnattachedComment, EachPackageWithIdentifiersCallback,
    cloneCommentGroupBefore, resolveNotFound, splitParenthesizedOrGenericReceiver, resolveOnePackage (9.2k to ... [line elided]
```

Do 2 things:
1. triple line length before elision
2. when elision, just append "...", not "... [line elided]"

process mods:
- do everyine in one turn
- skip `review` tool
- before check_spec_conformance, validate your work by running ~ `go run . exec` to observe output. Iterate if it doesn't work.

## Plan [DONE]

### Package internal/tools/cli [DONE]

- Update visible stdout line elision to allow roughly three times the current line length before eliding.
- Change long-line elision marker to append `...` instead of `... [line elided]`.
- Add/adjust focused tests around long visible-output lines.
- No `SPEC.md` change planned: `internal/tools/cli/SPEC.md` already requires visible stdout to be bounded and may be elided.

### Validation [DONE]

- Run package tests for `internal/tools/cli`.
- Run `go run . exec` before SPEC conformance, per user request, and observe the visible output behavior.
- Run `check_spec_conformance({"only_changed":true})`.
- Skip the `review` tool per user request.

## Review

- `review` tool skipped per user request.
- `go test ./internal/tools/cli`: passed.
- `go run . exec --yes --no-color "Use the codalotl_cli tool to run its help catalog. Do not edit files."`: passed, showed streamed `codalotl_cli` output and no edits.
- `check_spec_conformance({"only_changed":true})`: passed for `internal/agent` and `internal/tools/cli`.

## Summary

- Increased visible stdout long-line limit from 400 runes to 1200 runes.
- Changed per-line visible stdout elision marker from ` ... [line elided]` to `...`.
- Added focused tests for max-length and over-max line sanitization behavior.
- Recorded SPEC conformance CAS entries for changed packages.

## State

- Active branch: `jn/docubot-ellision`.
- Active PR file: `.prs/2026-05-08_1778259412_docubot-ellision.md`.
- Requested behavior is located in `internal/tools/cli/cli.go` visible stdout sanitization (`visibleOutputMaxLineRunes`, `writeVisibleToken`).
- Current branch already contains commit `00f124a agent: add docs`, which only adds doc comments in `internal/agent`.
- Implementation commit: `dd4c178 Adjust visible output line elision`.
