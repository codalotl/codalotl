# PR

## User Summary (do not modify)

When I run the refactor tool, I see something like this:

```
• Refactoring docs-add in internal/gocode
  • Need docs for 16 identifiers
  • > Requesting docs for 12 identifiers: FilterIdentifiersOptions, FilterIdentifiersOptionsAll, FilterIdentifiersOptionsAllNonGenerated,
    FilterIdentifiersOptionsDocumentedNonAmbiguous, FilterIdentifiersOptionsNonAmbiguous, ErrResolveNotFound, UnattachedComment, EachPackageWithIdentifiersCallback,
    cloneCommentGroupBefore, resolveNotFound, splitParenthesizedOrGenericReceiver, resolveOnePackage (9.2k to ... [line elided]
```

Do 2 things:
1. triple line length before elision
2. when elision, just append "...", not "... [line elided]"

process mods:
- do everyine in one turn
- skip `review` tool
- before check_spec_conformance, validate your work by running ~ `go run . exec` to observe output. Iterate if it doesn't work.
