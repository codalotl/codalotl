# PR

## User Summary (do not modify)

Add support for gpt-5.6. The pricing is as follows:

```
    {
      "id": "gpt-5.6-sol",
      "cost_per_1m_in": 5,
      "cost_per_1m_out": 30,
      "cost_per_1m_in_cached": 0.5,
      "cost_per_1m_out_cached": 0,
      "cost_per_1m_in_save_to_cache": 6.25,
      "context_window": 1050000,
      "max_output": 128000
    },
    {
      "id": "gpt-5.6-terra",
      "cost_per_1m_in": 2.5,
      "cost_per_1m_out": 15,
      "cost_per_1m_in_cached": 0.25,
      "cost_per_1m_out_cached": 0,
      "cost_per_1m_in_save_to_cache": 3.125,
      "context_window": 1050000,
      "max_output": 128000
    },
    {
      "id": "gpt-5.6-luna",
      "cost_per_1m_in": 1,
      "cost_per_1m_out": 6,
      "cost_per_1m_in_cached": 0.1,
      "cost_per_1m_out_cached": 0,
      "cost_per_1m_in_save_to_cache": 1.25,
      "context_window": 1050000,
      "max_output": 128000
    }
```

## Plan

### Package internal/llmmodel [DONE]

- Add the three GPT-5.6 OpenAI provider models to `internal/llmmodel/config/openai.json`:
  - `gpt-5.6-sol`
  - `gpt-5.6-terra`
  - `gpt-5.6-luna`
- Preserve the current default model (`gpt-5.5-high`) unless implementation discovers existing package conventions requiring otherwise.
- Keep these model IDs directly selectable as given in the user summary, not synthesized into `-medium`/`-high`/`-xhigh` variants.
- Ensure model metadata exposes the requested pricing, cache-write pricing, context window, and max output.
- Add or update focused package tests covering the new model IDs and representative metadata.

No `SPEC.md` change planned: the existing `llmmodel` spec already covers catalog-backed default model loading and does not enumerate every OpenAI catalog entry.

## Review

- Code review vs `main`: no findings; verdict `patch is correct`.
- SPEC conformance (`check_spec_conformance --only-changed`): failed for `internal/llmmodel`.
  - Non-latent minor issue: `SPEC.md` and public docs still describe `CostPer1MInSaveToCache` as Anthropic-only, but GPT-5.6 OpenAI models intentionally include nonzero cache-write pricing.
  - Action: update `internal/llmmodel` SPEC/API docs so cache-write pricing is provider-neutral, then rerun conformance.
  - SPEC edit reviewed: provider-neutral `CostPer1MInSaveToCache` wording is coherent and correctly located.
  - [DONE] API docs updated in `6272398`; rerun conformance next.
- Clarify public API doc-improvement workflow for `internal/llmmodel`: no opportunities found.
- Rerun code review vs `main`: no findings; verdict `patch is correct`.
- Rerun SPEC conformance (`check_spec_conformance --only-changed`): `internal/llmmodel` conforms.

## Summary

TBD.

## State

- Branch: `jn/add-5.6`
- Workspace was clean before planning.
- Relevant package: `internal/llmmodel`
- OpenAI catalog file: `internal/llmmodel/config/openai.json`
- Relevant tests: `internal/llmmodel/llmmodel_test.go`
- `internal/llmmodel/SPEC.md` says OpenAI default large GPT-5 model and `gpt-5.3-codex` get reasoning variants; other OpenAI models are registered directly via provider model ID.
- Implementation commit: `920c58f` adds direct catalog entries and tests for GPT-5.6 Sol/Terra/Luna.
- Validation: `go test ./internal/llmmodel` passed.
- SPEC conformance follow-up: `internal/llmmodel/SPEC.md` now documents cache-write pricing as provider-neutral.
- API-doc follow-up: `internal/llmmodel/llmmodel.go` now matches provider-neutral cache-write pricing docs.
- Latest validation: review clean, SPEC conformance passing, CAS conformance artifact generated.
