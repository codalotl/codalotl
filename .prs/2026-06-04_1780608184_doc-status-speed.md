# PR

## User Summary (do not modify)

Improve the performance of `codalotl docs status`. Keep behavior the same. Only implement an optimization if it doesn't increase technical debt.

## Plan

### Phase 0: Measure before optimizing [DONE]

Measured `codalotl docs status` from a built binary so compile time is excluded:

- Run 1: real 24.23s, user 34.11s, sys 40.16s.
- Run 2: real 24.03s, user 33.99s, sys 40.47s.
- Run 3: real 24.09s, user 34.34s, sys 40.70s.

Measured `internal/cli` status phases across 71 packages:

- Package discovery: 0.23s.
- Package loading: 0.14s.
- `docs_add`: 17.07s.
- `docs_fix`: 0.02s.
- `reflow`: 5.73s.

Measured `internal/docubot` important-docs status path:

- Static important identifier selection: 1.56s.
- `gocodecontext.Groups`: 15.23s.
- Exact `CountMissingDocs` important count: 12.11s.

Conclusion: primary bottleneck is docs-add status, specifically exact important-docs counting and repeated graph grouping. Reflow is secondary. The previous reflow-first plan was wrong.

### Phase 1: Package `internal/docubot` [DONE]

Add a boolean documentation-status API for `AddDocs` target selection, as described in `internal/docubot/SPEC.md`.

- Keep `CountMissingDocs` exact and preserve existing `AddDocs` target semantics.
- For `OnlyDocumentImportantIdentifiers`, answer "does this package need docs?" by checking cheap/static important targets first.
- Only run expensive `gocodecontext.Groups` work when static important targets are fully documented and graph-selected important identifiers can affect the boolean answer.
- Keep status checks read-only and LLM-free.

Implemented in commit `7d753ee`. `go test ./internal/docubot` passed.

### Phase 2: Package `internal/cli` [DONE]

Use the new `docubot` boolean status helper for the `docs_add` column.

- Preserve `codalotl docs status` output, package discovery behavior, status semantics, and read-only behavior.
- Keep `docs_fix` and `reflow` behavior unchanged in this phase.
- Remeasure after implementation using the same built-binary timing and phase breakdown.

Implemented in commit `73cfdb0`. `go test ./internal/cli` passed.

Post-Phase-2 built-binary measurements:

- Run 1: real 19.99s, user 27.79s, sys 30.36s.
- Run 2: real 19.60s, user 27.68s, sys 30.75s.
- Run 3: real 19.87s, user 27.70s, sys 30.61s.

Post-Phase-2 phase breakdown:

- Package discovery: 0.24s.
- Package loading: 0.15s.
- `docs_add`: 12.90s.
- `docs_fix`: 0.02s.
- `reflow`: 5.96s.

### Phase 3: Optional reflow follow-up [DONE]

Only optimize reflow if post-Phase-2 measurements still show it is material and the fix is clean.

- Likely owner is `internal/cli` if the right fix is batching/shared package clones.
- Consider `internal/updatedocs` only if a better dry-run status API is warranted.
- Do not implement a reflow optimization speculatively.

Post-Phase-2 measurements showed reflow was still material at ~6s, so I tried a clean `internal/cli` batching/shared-clone implementation. It preserved behavior but only improved built-binary runtime from ~19.8s to ~19.1s and left phase-level reflow at ~5.9s. That is not enough benefit for the added complexity, so the batching implementation was reverted in commit `1686b0e`.

## Review

Not run yet.

## Summary

TBD.

## State

- Active branch: `jn/doc-status-speed`.
- Active PR file: `.prs/2026-06-04_1780608184_doc-status-speed.md`.
- User goal: speed up `codalotl docs status` while keeping behavior identical and avoiding technical debt.
- Main CLI code: `internal/cli/docs_status.go`.
- Main docs-add status code: `internal/docubot/count_missing_docs.go`, `internal/docubot/important.go`.
- Relevant tests: `internal/cli/docs_status_test.go`.
- Relevant behavior spec: `internal/cli/SPEC.md` lines for `codalotl docs status` say columns are `package`, `docs_add`, `docs_fix`, `reflow`; values are `current`, `needed`, `error`; command is read-only.
- Measurements: built-binary `codalotl docs status` is consistently ~24s on this repo. Phase breakdown: docs_add ~17.1s, reflow ~5.7s, discovery/load/docs_fix <0.5s total.
- `docsAddStatus` originally called `docubot.CountMissingDocs(...OnlyDocumentImportantIdentifiers...)` and only needed `missing > 0`.
- Important-docs status spends most time in `gocodecontext.Groups`; static important selection is much cheaper.
- Package ownership decision: optimize docs-add status in `internal/docubot`; integrate it in `internal/cli`; defer any `internal/gocodecontext` or reflow work until post-fix measurement justifies it.
- `internal/docubot` now exposes `NeedsDocs` and fast-paths important-mode boolean status before graph grouping where possible.
- `internal/cli` now uses `docubot.NeedsDocs` for `docs_add`; built-binary runtime improved from ~24.1s to ~19.8s.
- Reflow batching attempt did not produce enough improvement to justify the extra complexity; leave reflow unchanged unless a deeper `updatedocs`-owned optimization is separately measured and designed.

## Learnings

- Reflow's cost is not primarily independent package cloning. A shared-clone/batched CLI implementation preserved behavior but barely moved phase timing, so it was reverted to avoid technical debt.
