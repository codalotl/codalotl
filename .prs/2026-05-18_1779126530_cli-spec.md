# PR

## User Summary (do not modify)

`product-spec` defines how I want package params to work in the CLI. Update the CLI to conform to this spec. Don't worry about non-CLI things.

## Plan

### Package `internal/cli`
- Align CLI `<path/to/pkg>` handling with `product-spec/features/cli.md`.
- Update shared package argument resolution so import paths take precedence over bare CWD-relative fallback dirs, while explicit relative paths and absolute dirs stay direct filesystem paths.
- Keep `...` rejected for ordinary single-package parameters, while preserving commands that explicitly accept package patterns.
- Update help text and tests for package parameter precedence and accepted forms.

## Review

Not run yet.

## Summary

Pending.

## State

- Active PR file: `.prs/2026-05-18_1779126530_cli-spec.md`.
- Branch: `jn/cli-spec`.
- User goal: make CLI package parameters conform to `product-spec/features/cli.md`; non-CLI package semantics are out of scope.
- `internal/cli/pkgload.go` currently checks existing filesystem dirs before import paths, contradicting product-spec precedence for bare/slashed args like `fmt`.
- `internal/cli/commands.go` has shared package arg help text and `resolveSpecPathArg`; both likely need alignment.
