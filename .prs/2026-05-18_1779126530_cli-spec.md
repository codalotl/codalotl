# PR

## User Summary (do not modify)

`product-spec` defines how I want package params to work in the CLI. Update the CLI to conform to this spec. Don't worry about non-CLI things.

## Plan

### Package `internal/cli` [DONE]
- Align CLI `<path/to/pkg>` handling with `product-spec/features/cli.md`.
- Update shared package argument resolution so import paths take precedence over bare CWD-relative fallback dirs, while explicit relative paths and absolute dirs stay direct filesystem paths.
- Keep `...` rejected for ordinary single-package parameters, while preserving commands that explicitly accept package patterns.
- Update help text and tests for package parameter precedence and accepted forms.

### Package `internal/cli` follow-up [DONE]
- Verify/fix bare CWD-relative fallback dirs when the current working directory is not itself inside a Go module but the target package dir is. This may matter because product-spec allows sandbox dirs that contain, but are not equal to, a project's `go.mod`.

## Review

Review against `origin/jn/cli-spec` found two actionable P2 issues:
- [DONE] Resolve import paths even when cwd is outside any Go module. Current code falls back to a local dir such as `fmt` instead of resolving stdlib `fmt`, and rejects `fmt` if no local dir exists.
- [DONE] Do not ignore real module-loading errors for explicit/fallback dirs under malformed `go.mod`; only the no-enclosing-go.mod case should use module-less package loading.

SPEC conformance:
- `check_spec_conformance({"only_changed":true})`: `internal/cli` conforms. CAS record written under `.codalotl/cas/specconforms-1`.

Second review after fixes found one actionable P2 issue:
- [TODO] Normalize module-root import relative dirs: when an import path resolves to packageRelDir `.`, load it as module root `""` so later reload/clone paths do not produce import paths like `example.com/app/.`.

Second SPEC conformance:
- `check_spec_conformance({"only_changed":true})`: `internal/cli` has only latent minor nonconformances around PreferredProvider/PreferredModel config behavior. These predate this PR and are out of scope for CLI package parameter semantics.

## Summary

Pending.

## State

- Active PR file: `.prs/2026-05-18_1779126530_cli-spec.md`.
- Branch: `jn/cli-spec`.
- User goal: make CLI package parameters conform to `product-spec/features/cli.md`; non-CLI package semantics are out of scope.
- `internal/cli/pkgload.go` currently checks existing filesystem dirs before import paths, contradicting product-spec precedence for bare/slashed args like `fmt`.
- `internal/cli/commands.go` has shared package arg help text and `resolveSpecPathArg`; both likely need alignment.
- Implementation commit `7599a97` updates shared package loading, `spec fmt/diff` path resolution, and `exec --package` path normalization; `go test ./internal/cli` passed.
- Ran `refactor docs-improve-from-clarify` for pending `clarify-public-api-1` CAS targeting `internal/gocas`; tool reported no opportunity and made no edits.
- Follow-up commit `f39f43b` handles bare fallback dirs when cwd is outside a module but target dir is inside a nested module; `go test ./internal/cli`, `go test ./internal/gocode`, and `git diff --check` passed.
- A new clarify CAS record for `internal/gocode` appeared during follow-up; `refactor docs-improve-from-clarify` consumed it by improving `NewModule`/`ErrResolveNotFound` docs.
- Review-fix commit `41546c8` resolves stdlib import paths outside modules, preserves import precedence over local dirs, returns malformed module errors for directory args, and consumes another `internal/gocode` clarify CAS doc improvement; `go test ./internal/cli`, `go test ./internal/gocode`, and `git diff --check` passed.
