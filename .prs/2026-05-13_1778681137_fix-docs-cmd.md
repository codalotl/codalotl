# PR

## User Summary (do not modify)

Codalotl allows users to find and fix documentation errors in their codebase:
- Documentation is just godoc-style comments on top-level identifiers (not internal function comments) - see `docs.md`.
- Documentation errors are detected per-package. Running it on a package finds/fixes errors in the non-test code, the test code, and any `_test` blackbox package.
- Each found documentation error is automatically fixed.
- It is incredibly important that this does NOT triggle false positives. If users find it annoying, it's useless.
    - Documentation errors are ONLY saying something that is materially false.
    - It does NOT flag omissions.
    - It permits imprecise language.
    - Lack of documentation is not an error (identifiers without docs aren't scanned).
- One conceptual test: fixing docs should be idempotent - the second run should not find any errors.
- After running on a package, it writes a CAS file, hashed against the fixed code, indicating the package has been processed.
    - The CAS file should be written via manual CLI invocation, `codalotl_cli`, and `refactor`.

CLI commands:
- `codalotl docs fix path/to/pkg`: find and fix all doc errors in given package.
- `codalotl docs fix --identifiers="foo,bar" path/to/pkg`: limit identifiers checked to foo and bar.

- This CLI command is available in the `codalotl_cli` tool.
- It is also available in the refactor tool, as `docs-fix`.
- When the orchestrator calls either, the tokens used are added to the agents total, as displayed in the TUI.
- The orchestrator can easily be instructed to run this. Ex: `run refactor(docs-fix, internal/some/pkg)`.
    - The orchestrator knows to commit the CAS entries.

The above is the end state I desire. I think most of this is already in. Your job is the plumbing.
- keep docubot ~as-is, if we need to improveit, that's a separate PR
- wire into CLI
- wire into codalotl_cli
- wire into refactor

Validation:
- after implementation phase, you must actually try to run ~this: `go run . docs fix internal/something`.
- Also verify the orchestrator actually works. Use something like `go run . exec`.
- You can test on this repo, but dont commit the results (tell orchestrator not to commit anything).
- iterate until it works.

## Plan

### Existing docubot groundwork [DONE]

- Branch already adds/fixes `docubot` package docs-error APIs and the development `internal/docubot/cmd fix` runner.
- Keep docubot behavior mostly as-is; this PR is plumbing.

### Package `internal/cli` [DONE]

- Add `codalotl docs fix [--identifiers <comma-list>] <path/to/pkg>`.
- Command should call `docubot.FindAndFixDocErrors`.
- It should scan non-test, in-package test, and black-box `_test` package docs through docubot.
- It should not add missing docs or chase non-material wording issues.
- It should write a package CAS record under `docs-fix-1` after successful runs, keyed against fixed code.
- CAS metadata should distinguish whole-package runs from identifier-limited runs so identifier-limited runs do not falsely mark the entire package processed.
- Add the command to the `codalotl_cli` whitelist.

### Package `internal/tools/refactor` [DONE]

- Add a `docs-fix` refactor.
- Delegate to `codalotl_cli` with `docs fix <package>`.
- Report edited files and the saved CLI CAS record path in the refactor result.

### Validation [DONE]

- Run focused package tests for changed packages.
- Run `go test ./...` if practical.
- Manually run `go run . docs fix internal/something`; do not commit incidental doc edits or test CAS unless they are intentional validation artifacts.
- Verify orchestrator/tool plumbing with `go run . exec ...` and do not let that test commit incidental changes.

## Review

Full code review not run yet.

SPEC conformance only (2026-05-13):
- `check_spec_conformance({"only_changed": true})` passed.
- Conforming changed packages: `internal/agent`, `internal/cli`, `internal/docubot`, `internal/docubot/cmd`, `internal/tools/refactor`.
- Wrote `specconforms-1` CAS records for the conforming packages.

## Summary

TBD.

## State

- Active branch: `jn/fix-docs-cmd`.
- Active PR file: `.prs/2026-05-13_1778681137_fix-docs-cmd.md`.
- Existing branch commits already touched `internal/docubot`, `internal/docubot/cmd`, `internal/gocode`, and `internal/agent`.
- Missing plumbing observed before this plan: `internal/cli/SPEC.md` did not mention `docs fix` or whitelist exposure; `internal/tools/refactor/SPEC.md` did not mention `docs-fix`.
- SPEC reviews for `internal/cli` and `internal/tools/refactor` accepted the new design. Main implementation watchpoint: make the saved docs-fix CAS path available to `refactor` after delegated CLI execution.
- Commit `6c586be` added `codalotl docs fix`, whitelisted it in `codalotl_cli`, wrote `docs-fix-1` package CAS records, and emits `docs_fix_result=<json>` on stdout for downstream parsing.
- Commit `66ecc0b` added the `docs-fix` refactor, delegates through `codalotl_cli`, parses `docs_fix_result=<json>`, and returns the delegated CAS record path.
- Validation passed:
  - `go test ./internal/cli ./internal/tools/refactor`
  - `go test ./...`
  - In a temporary worktree, `go run . docs fix internal/q/semver` exited 0, found no issues, applied 0 fixes, and wrote a `docs-fix-1` CAS record.
  - In the same temporary worktree, `go run . exec "Validation only..."` exited 0 and returned `OK`.
- Temporary worktree validation artifacts were removed; current workspace remained clean.
- Conformance-only check passed for changed packages: `internal/agent`, `internal/cli`, `internal/docubot`, `internal/docubot/cmd`, and `internal/tools/refactor`.
