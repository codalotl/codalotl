# PR

## User Summary (do not modify)

For openai, in llmstream, we send the system prompt as input item whose type is "message" and whose role is "system".

I want to change this to use 'instructions' instead. Populate this field on every create response request (with the same string).

Validation:
- in addition to normal tests,
- do something like `go run . exec` to do something, make sure there's tool calls, this hsouldn't just be a one-and-done request. Validate that caching/token usage looks good (and the same) both before and after the change. this uses real tokens. that's fine.

## Plan

### Package `internal/llmstream` [DONE]
- Update OpenAI Responses request construction so the initial system turn is always sent through root `instructions`.
- Stop encoding system turns in `input`, including full-history/no-store replay.
- Preserve existing linking/no-store history behavior, prompt cache key behavior, and tool/result replay.
- Update focused request-shape tests and live OpenAI validation around multi-turn tool flow/cached input.

### Package `internal/noninteractive/integration` [DONE]
- Update replay fixture generation/tests if request-shape expectations need to account for root `instructions`.

### Pre-review validation [DONE]
- `go test -count=1 ./internal/llmstream ./internal/noninteractive/integration`
- `go test -count=1 ./...`
- Live before/after `go run . exec` validation with `gpt-5-mini`, real OpenAI tokens, and tool calls.

## Review

## Summary

## State

- Current branch: `jn/openai-instructions`.
- Primary package: `internal/llmstream`.
- Implementation commit `aa9f3ab`:
  - `internal/llmstream/open_ai_responses.go` sets `ResponseNewParams.Instructions` from the conversation system message on every OpenAI Responses create request.
  - OpenAI input encoding skips `RoleSystem` turns, including no-store full-history replay.
  - Request-shape tests assert root instructions and no system-role input items for linked/default and no-store paths.
  - Noninteractive replay fixtures now expect root `instructions`.
- OpenAI SDK `responses.ResponseNewParams` has `Instructions param.Opt[string]`; SDK docs note `instructions` are not carried across `previous_response_id`, so every request must set it.
- Live validation logs:
  - Before implementation: 4 requests; no root instructions; first request had system input item; cached input total 8576.
  - After implementation: 3 requests; every request had 9309-byte root instructions; no system input items; cached input total 8576.
  - After run token table: total input 13588, cached input 8576, output 316.
