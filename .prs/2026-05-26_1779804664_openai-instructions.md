# PR

## User Summary (do not modify)

For openai, in llmstream, we send the system prompt as input item whose type is "message" and whose role is "system".

I want to change this to use 'instructions' instead. Populate this field on every create response request (with the same string).

Validation:
- in addition to normal tests,
- do something like `go run . exec` to do something, make sure there's tool calls, this hsouldn't just be a one-and-done request. Validate that caching/token usage looks good (and the same) both before and after the change. this uses real tokens. that's fine.
