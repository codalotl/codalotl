# PR

## User Summary (do not modify)

read product-spec/features/cas.md - this pr will move closer to this

- implement `codalotl cas ls-stale`

validation:
- tests as normal
- actually try out `codalotl cas ls-stale` by running `go run . cas ls-stale ...`
    - notice things about it
    - do real Q/A
    - iterate on ux if it doesn't seem good (but stick to spec)

## Plan

### Package internal/cli

- Replace `codalotl cas ls-unset` with `codalotl cas ls-stale`.
- Implement `ls-stale` against registered CAS namespaces and module packages.
- Output only stale package paths, one per line, using module-relative `./path` display.
- Support `--stale-after-days` and `--min-churn-percent`; combine both filters with OR when both are present.
- Treat packages with no CAS record history as always stale and always output.
- Use existing CAS package summary data for current/prior/churn state; avoid new lower-level CAS APIs unless needed.
- Validate with focused CLI tests, `go test ./internal/cli`, `go test ./internal/gocas`, and manual `go run . cas ls-stale ...` Q/A.

## Review

Not run yet.

## Summary

TBD.

## State

- Branch: `jn/cas-stale`.
- PR file: `.prs/2026-05-19_1779195152_cas-stale.md`.
- Product spec: `product-spec/features/cas.md`.
- Main target package: `internal/cli`.
- Existing CLI has `cas ls-summary` and `cas ls-unset`; `ls-summary` already uses `gocas.DB.SummarizePackage`.
