# PR

## User Summary (do not modify)

read product-spec/features/cas.md - this pr will move closer to this

- implement `codalotl cas ls-stale`

validation:
- tests as normal
- actually try out `codalotl cas ls-stale` by running `go run . cas ls-stale ...`
    - notice things about it
    - do real Q/A
    - iterate on ux if it doesn't seem good (but stick to spec)

## Plan

### Package internal/cli [DONE]

- [DONE] Replace `codalotl cas ls-unset` with `codalotl cas ls-stale`.
- [DONE] Implement `ls-stale` against registered CAS namespaces and module packages.
- [DONE] Output only stale package paths, one per line, using module-relative `./path` display.
- [DONE] Support `--stale-after-days` and `--min-churn-percent`; combine both filters with OR when both are present.
- [DONE] Treat packages with no CAS record history as always stale and always output.
- [DONE] Use existing CAS package summary data for current/prior/churn state; avoid new lower-level CAS APIs unless needed.
- [DONE] Validate with focused CLI tests, `go test ./internal/cli`, `go test ./internal/gocas`, and manual `go run . cas ls-stale ...` Q/A.

### Validation

- `go test ./internal/cli`: pass.
- `go test ./internal/gocas`: fails in existing `SummarizePackage` tests around CAS-add commit time/churn discovery; no `internal/gocas` code changed in this PR.
- `git diff --check`: pass.
- Manual Q/A:
  - `go run . cas ls-stale docs-fix` prints module-relative stale packages, one per line, including `.` and `./internal/cli`.
  - `go run . cas ls-stale docs-fix --stale-after-days=-1` exits with usage error: `invalid --stale-after-days: must be >= 0`.

## Review

Full review not run yet per user request.

SPEC conformance (`only_changed=true`):

- `internal/cli`: conforms.

## Summary

TBD.

## State

- Branch: `jn/cas-stale`.
- PR file: `.prs/2026-05-19_1779195152_cas-stale.md`.
- Product spec: `product-spec/features/cas.md`.
- Main target package: `internal/cli`.
- Existing CLI has `cas ls-summary` and `cas ls-unset`; `ls-summary` already uses `gocas.DB.SummarizePackage`.
- `cas ls-stale` implemented in `internal/cli`; `cas ls-unset` removed as a user-facing command.
