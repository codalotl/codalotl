# PR

## User Summary (do not modify)

add the refactor tool to the orchestrator agent.

do not do use the review tool. only check spec conformance.

Do your entire workflow in one turn.

## Plan

### Package internal/agentbuilder [DONE]
- Add optional externally supplied `refactor` tool to `pr-orchestrator`.
- Keep `pr-orchestrator` behavior unchanged when no `refactor` override is registered.
- Update package docs/tests as needed to cover orchestrator tool exposure.

### Validation [DONE]
- Do not run the `review` tool per user instruction.
- Run tests relevant to `internal/agentbuilder`: `go test ./internal/agentbuilder`.
- Run `check_spec_conformance` only.

## Review

- User override: skip `review` and `review_spec_changes`; validate with tests and SPEC conformance only.

## Summary

## State

- Active PR file: `.prs/2026-05-07_1778175366_refactor-orch.md`
- Active branch: `jn/refactor-orch`
- Located change in `internal/agentbuilder`; embedded YAML defines `pr-orchestrator` tools in `internal/agentbuilder/data/config.yml`.
- `internal/agentbuilder/SPEC.md` now says `pr-orchestrator` includes optional `refactor`.
- Implementation commit `654fd36` adds `refactor` to `pr-orchestrator` YAML tools, updates docs, and covers override/no-override behavior.
