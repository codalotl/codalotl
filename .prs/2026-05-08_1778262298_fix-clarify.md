# PR

## User Summary (do not modify)

It looks like the clarify_public_api tool just has read and ls as tools?

I want it to be able to use get_public_api and clarify_public_api as well.

process overrides:
- do everything in one turn.
- after implementation, actually validate it using ~ `go run . exec` -- if clarify makes edits, don't commit them.
- do check_spec_conformance, skip review tool.

## Plan [DONE]

### Package `internal/agentbuilder` [DONE]
- Update `clarify_public_api` agent/toolset requirements so the clarifying subagent can inspect public docs with `get_public_api` and delegate follow-up clarification with `clarify_public_api`, while remaining read-only.
- Implement by expanding the toolset supplied to clarify subagents and updating tests/documentation that assert built-in tool lists and prompt wording.

### Package `internal/tools/pkgtools` [DONE]
- No implementation expected. `clarify_public_api` tool already supports nested subagent execution and docs-improvement behavior; only its supplied clarify-agent toolset needs to change.

### Validation
- [DONE] Run targeted `go test` for `internal/agentbuilder`.
- [DONE] Run `go run . exec` validation for a prompt that forces package-mode `clarify_public_api` to use `get_public_api` and `clarify_public_api`; if docs-improvement edits occur, discard them before committing.
- [DONE] Run `check_spec_conformance` with `only_changed=true`; skip `review` per user override.

## Review

Not run; user requested skipping the review tool.

SPEC conformance: `check_spec_conformance({"only_changed": true})` reports `internal/agentbuilder` conforms.

## Summary

This PR expands the built-in `clarify_public_api` agent toolset so clarify subagents can inspect public package APIs with `get_public_api` and delegate follow-up API questions with nested `clarify_public_api`, while preserving the read-only clarify toolset.

Validation:
- `go test ./internal/agentbuilder`
- `go run . exec --json --yes --package internal/agentbuilder ...` confirmed the clarify subagent used `get_public_api` and nested `clarify_public_api`; validation-only docs edits were discarded.
- `check_spec_conformance({"only_changed": true})` reports `internal/agentbuilder` conforms.

The review tool was skipped per user request.

## State

- Active branch: `jn/fix-clarify`.
- PR file: `.prs/2026-05-08_1778262298_fix-clarify.md`.
- Implementation package: `internal/agentbuilder`.
- Commit `b02bce1` expands the clarify-agent tool list to `read_file`, `ls`, `get_public_api`, `clarify_public_api`; `go test ./internal/agentbuilder` passed.
- `go run . exec --json --yes --package internal/agentbuilder ...` validated that a package-mode agent can call `clarify_public_api`, and the clarify subagent can call both `get_public_api` and a nested `clarify_public_api`; the nested clarify subagent also called `get_public_api`.
- The exec validation produced docs-improvement edits to `internal/agentbuilder/agentbuilder.go` and `internal/agentbuilder/SPEC.md`; these were discarded per the user override and are not part of the PR.
