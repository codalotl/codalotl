# PR

## User Summary (do not modify)

It looks like the clarify_public_api tool just has read and ls as tools?

I want it to be able to use get_public_api and clarify_public_api as well.

process overrides:
- do everything in one turn.
- after implementation, actually validate it using ~ `go run . exec` -- if clarify makes edits, don't commit them.
- do check_spec_conformance, skip review tool.
