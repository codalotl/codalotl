# PR

## User Summary (do not modify)

Update the `codalotl pr refactor` cli command to conform to this spec: product-spec/features/pr-orchestrator.md

## Plan

### Package internal/cli [DONE]

- [DONE] Update `codalotl pr refactor` to accept exactly one package selector: `--package=<path/to/pkg>` or `--all-packages`.
- [DONE] Add optional `--refactor=<name>` validation for supported refactor names: `docs-add`, `docs-fix`, `dry`, `test-cleanup`, `test-ensure-coverage`.
- [DONE] Preserve existing single-package behavior when `--package` is supplied without `--refactor`.
- [DONE] Support `--package=<path/to/pkg> --refactor=<name>` by generating a single-package PR file for only that refactor.
- [DONE] Support `--all-packages --refactor=<name>` by generating a PR file targeting all Go packages in the current module.
- [DONE] Reject `--all-packages` without `--refactor` because that PR would be too large.
- [DONE] Generate feature names matching the product spec, including `refactor-docs-fix-all-packages` for `--all-packages --refactor=docs-fix`.
- [DONE] Keep startup behavior command-specific: `pr refactor` does not require LLM configuration or startup tool validation.
- [DONE] Update tests around argument validation, generated PR-file instructions, feature names, and startup-validation bypass.

Current phase is only `internal/cli`; no new package is needed.

Implementation committed in `ab81bd1`. Verified with `go test ./internal/cli` and `git diff --check`.

## Review

Manual validation before formal review:
- [DONE] Ran `go run -exec <chdir-wrapper> . pr refactor --package=internal/mypkg --refactor=docs-fix` in a throwaway git repo. It created branch `jt/refactor-docs-fix-internal-mypkg`, committed the PR file, printed a concise `Created .prs/...` line, and generated a focused single-refactor PR file with `docs-fix` CAS recertification guidance.
- [DONE] Ran `go run -exec <chdir-wrapper> . pr refactor --package=./internal/mypkg` in a throwaway git repo. It preserved existing all-refactors package behavior and generated the expected ordered refactor instructions.
- [DONE] Ran `go run -exec <chdir-wrapper> . pr refactor --all-packages --refactor=dry` in a throwaway git repo. It created branch `refactor-dry-all-packages`, committed the PR file, printed a concise `Created .prs/...` line, and generated all-packages instructions that look usable for orchestrator follow-up.
- [DONE] Ran validation-error cases with `go run` for `--all-packages` without `--refactor`, both selectors together, and unsupported `--refactor=nope`. They failed before creating `.prs` files and showed the intended usage errors.

Formal review against `main`:
- Review verdict: patch is incorrect.
- Finding: `--all-packages --refactor=<name>` can scaffold a PR outside a Go module. The command should validate there is a current Go module before creating/committing an all-packages PR file.

SPEC conformance with `check_spec_conformance({"only_changed":true})`:
- `internal/cli`: nonconforming, non-latent minor issue.
- Finding: package-without-`--refactor` PR template should explicitly state the selected all-refactors flow and mention skipping no-op refactor results, matching the updated `internal/cli/SPEC.md`.

Follow-up needed before completion:
- Validate current module for `--all-packages` flow before scaffolding.
- Polish single-package all-refactors PR template wording for selected flow/no-op skip guidance.

## Summary

Pending.

## State

- Branch: `jn/pr-refactor-all`
- PR file: `.prs/2026-05-28_1780001033_pr-refactor-all.md`
- User-facing product spec: `product-spec/features/pr-orchestrator.md`
- Updated `internal/cli/SPEC.md` to include `--all-packages` and `--refactor=<name>` behavior.
- Ran clarify-driven doc improvement workflow for pending `clarify-public-api` CAS records in `internal/subscriptions/openaisub`, `internal/gocas`, and `internal/q/cas`; all were no-ops and produced no workspace changes.
- Implementation commit `ab81bd1` updated `internal/cli/pr_new.go` and `internal/cli/pr_new_test.go`.
- `go test ./internal/cli` passed after implementation.
- `git diff --check` passed before implementation commit.
- Manual `go run` validation used temporary git repos under `.tmp-pr-refactor-manual`, inspected generated output/PR files, then removed the temp directory. Workspace was clean afterward.
- Formal review found an actionable all-packages module-validation bug.
- SPEC conformance found an actionable PR-template wording nonconformance in the single-package all-refactors flow.
