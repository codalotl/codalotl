# PR

## User Summary (do not modify)

situation:
- we have internal/docubot, which uses the LLM to generate documentation (AddDocs).
- this can be invoked in 2 ways from an agent: codalotl_cli, and the refactor tool.
- In both cases, since this doesn't use the typical `internal/agent`-style technology, the UI in tui/noninteractive don't show tokens used/cost.

solution:
- I want it to display the tokens/cost in TUI. it doesn't take up context window, obv, but it does increase tokens/cost.

process:
1. come up with a few technical alternatives for me to choose between
    - place options in this file, I'll tell you my choice as a message in this chat
2. plan+impl
3. Validation
    - you must validate that this works by actually trying it
    - run something like `go run . exec` with a prompt that causes a refactor tool call to document something small in this package (this actually uses tokens, so be judicious)
        - don't commit results
        - make sure tokens reported matches what you expect
4. Skip `review`, just do the spec conformance checks

