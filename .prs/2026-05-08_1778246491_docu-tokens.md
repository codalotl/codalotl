# PR

## User Summary (do not modify)

situation:
- we have internal/docubot, which uses the LLM to generate documentation (AddDocs).
- this can be invoked in 2 ways from an agent: codalotl_cli, and the refactor tool.
- In both cases, since this doesn't use the typical `internal/agent`-style technology, the UI in tui/noninteractive don't show tokens used/cost.

solution:
- I want it to display the tokens/cost in TUI. it doesn't take up context window, obv, but it does increase tokens/cost.

process:
1. come up with a few technical alternatives for me to choose between
    - place options in this file, I'll tell you my choice as a message in this chat
2. plan+impl
3. Validation
    - you must validate that this works by actually trying it
    - run something like `go run . exec` with a prompt that causes a refactor tool call to document something small in this package (this actually uses tokens, so be judicious)
        - don't commit results
        - make sure tokens reported matches what you expect
4. Skip `review`, just do the spec conformance checks

## Plan

### Step 1: technical alternatives [DONE]

User chose Option A.

#### Option A: agent-owned external LLM usage hook

Add an `agent` context helper for tool code to report token usage from LLM calls that happen outside the normal agent conversation. `docubot` would call that helper after each successful `llmstream.Completer.Complete` call, using the existing `BaseOptions.Context`.

- Shape:
  - `internal/agent`: add an exported no-op-safe helper such as `EmitExternalLLMUsage(ctx, modelID, usage)` and wire the tool-run context so it increments the owning agent's cumulative usage.
  - `internal/docubot`: after `Complete`, report the returned turn's `Usage`.
  - `internal/tui` / `internal/noninteractive`: mostly unchanged because their token displays already read `Agent.TokenUsage()`.
- Pros:
  - Correct architectural ownership: the agent owns usage accounting.
  - Covers both `codalotl_cli docs add` and `refactor docs-add`, because both ultimately pass the agent tool context into docubot.
  - Naturally covers future direct docubot calls inside agent tools.
- Cons:
  - Expands `internal/agent` public API for a non-conversation accounting path.
  - Needs care to not affect context-window usage; docubot tokens are cost tokens, not agent conversation context.

#### Option B: CLI-owned tracking completer

Keep `docubot` unchanged and have `codalotl docs add` pass a custom `llmstream.Completer` that wraps the default completer. The wrapper reports each returned turn's `Usage` to an agent context helper before returning the turn to docubot.

- Shape:
  - `internal/agent`: add a small no-op-safe external usage helper as in Option A.
  - `internal/cli`: add a tracking completer used only by `docs add`.
  - `internal/docubot`: no implementation changes.
- Pros:
  - Most targeted to the user-visible entrypoints: standalone `codalotl docs add`, `codalotl_cli`, and `refactor docs-add`.
  - Avoids coupling docubot itself to agent semantics.
  - Easier to test at the command boundary.
- Cons:
  - Future non-CLI docubot uses inside agent tools would need to remember to install the wrapper.
  - Usage reporting lives one layer away from the actual LLM call.

#### Option C: generic usage-reporting completer in `llmstream`

Add a reusable `llmstream` wrapper/decorator that invokes a callback after each successful completion. `codalotl docs add` installs that wrapper with a callback into `agent`.

- Shape:
  - `internal/llmstream`: add a `ReportingCompleter` / `CompleterFunc` helper.
  - `internal/agent`: add the external usage helper.
  - `internal/cli`: wrap the default completer for `docs add`.
- Pros:
  - Clean reusable primitive for any future one-shot LLM caller.
  - Keeps docubot decoupled from agent.
- Cons:
  - Slightly more abstraction than this PR needs.
  - Still requires callers to opt in to the wrapper.

#### Option D: parse/report docs command output

Have `docubot` print token/cost metadata to stdout, then teach `codalotl_cli` / `refactor` / TUI presentation to parse or surface it.

- Pros:
  - Avoids changes to `agent` internals.
- Cons:
  - Not recommended: brittle, duplicates token accounting/cost formatting, and does not update the existing TUI info-panel token totals naturally.

### Recommendation

Choose Option B unless you want docubot itself to always own reporting for all callers. Option B is the smallest change that covers both requested agent paths while preserving docubot's current API.

### Validation plan [DONE]

- Unit tests around the external usage helper and docubot usage reporting.
- Run focused package tests for changed packages.
- Run `check_spec_conformance({"only_changed":true})`.
- Manually validate with a judicious `go run . exec --yes ...` prompt that triggers `refactor` `docs-add` on a tiny package, then revert any generated documentation before committing.

### Step 2: implement Option A

#### Package internal/agent [DONE]
- Add a no-op-safe exported helper for tool code to report external LLM token usage from a `Run` context.
- Wire the tool-run context so reported usage accumulates into the owning agent's `TokenUsage()` and ancestor agents.
- Keep reported usage out of context-window accounting and conversation history.

#### Package internal/docubot [DONE]
- Report successful `llmstream.Completer.Complete` usage through `internal/agent` using `BaseOptions.Context`.
- Keep direct/non-agent docubot callers working as before.

#### Package internal/cli [DONE]
- No implementation change expected for Option A because `codalotl docs add` already passes command context into `docubot.BaseOptions.Context`.

## Review

Review is intentionally skipped per user request.

Validation:
- `go test ./internal/agent ./internal/docubot` passed.
- Manual validation passed with `go run . exec --yes --no-color --json "Please call the refactor tool exactly once with name docs-add and package internal/agent. Then stop after reporting the result."`
  - The run invoked `refactor` with `{"name":"docs-add","package":"internal/agent"}`.
  - The refactor output included docubot usage: `Requesting docs for 6 identifiers ... (8.6k toks)`.
  - The final noninteractive `done` event reported cumulative token usage `input=15345 output=3044 total=18389`, which includes the external docubot call.
  - The generated documentation edits were reverted and not committed.
- `go test ./...` passed.

SPEC conformance:
- `check_spec_conformance({"only_changed":true})` passed for:
  - `internal/agent`
  - `internal/agentbuilder`
  - `internal/docubot`
- The conformance run wrote CAS entries, which are included in this step.

## Summary

TBD.

## State

- Active PR file: `.prs/2026-05-08_1778246491_docu-tokens.md`.
- Branch: `jn/docu-tokens`.
- User wants token/cost from docubot `AddDocs` shown in TUI/noninteractive accounting when invoked through `codalotl_cli` or `refactor`.
- Current `docubot.BaseOptions.Context` is populated by `codalotl docs add` from the command context, which is the agent tool-run context when invoked via `codalotl_cli`.
- TUI token/cost panel already reads `Agent.TokenUsage()`; noninteractive final output also uses `Agent.TokenUsage()`.
- `internal/agent` now has `EmitExternalLLMUsage(ctx, usage)` wired into active tool-run contexts. Reported usage rolls up through `Agent.TokenUsage()` and does not change turns/context usage.
- `internal/docubot` now calls `agent.EmitExternalLLMUsage` after successful completions from `completeText`.

