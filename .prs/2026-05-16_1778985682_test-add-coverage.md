# PR

## User Summary (do not modify)

Implement the functionality for product-spec/features/refactors/test-ensure-coverage.md. This PR follows the prior one about test-cleanup. Follow sensibilities in that PR. Write prompts in a similar style.

Validation:
- in addition to normal tests, you MUST try this manually with something like `go run . exec`. Iterate.

Skip the `review` tool, but do `check_spec_coformance`. Do all steps in orchestrator workflow in one turn.

## Plan

### Package internal/tools/refactor
- Add a `test-ensure-coverage` prompt-style refactor using `limited_package_mode` and code-unit CAS caching.
- Add an embedded prompt in the same style as `test-cleanup`, but focused on measuring coverage, testing public APIs, and adding high-value missing/edge-case tests rather than reorganizing tests.
- Ensure tool metadata, prompt invocation, edited-file tracking, and CAS metadata are covered by focused tests.

### Validation
- Run focused package tests.
- Run normal tests.
- Manually smoke the workflow with a command like `go run . exec`.
- Run `check_spec_conformance` with `only_changed=true`.

## Review

Review tool intentionally skipped per user request.

## Summary

## State

- Branch: `jn/test-add-coverage`.
- Active PR file: `.prs/2026-05-16_1778985682_test-add-coverage.md`.
- Product spec: `product-spec/features/refactors/test-ensure-coverage.md`.
- Prior PR added `$go-testing` and `test-cleanup`; this PR should mirror its refactor-tool shape for `test-ensure-coverage`.
