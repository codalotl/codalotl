# PR

## User Summary (do not modify)

In docubot, when adding docs (or fixing/etc) for a package, I want the LLM to have SPEC.md context available to it. That being said, we can strip out any `## Public API` sections.

- gocodecontext shouldn't be changed.
- modify specmd package to provide a way of getting a SPEC.md without that section. Don't overthink it. Don't need to strip out all code blocks. Just the section if it's there.
- use that in docubot to supply llm with specmd context

## Plan

### Package internal/specmd
- Add a small public helper that returns SPEC.md contents with `## Public API` removed.
- Keep behavior scoped to the section removal the user requested; do not broaden into generic markdown/code-block stripping.
- Update `internal/specmd/SPEC.md` to document the helper.

### Package internal/docubot
- Add `internal/specmd` as docubot-owned context assembly, leaving `internal/gocodecontext` unchanged.
- Include package `SPEC.md` context in AddDocs LLM user context when a package SPEC exists, using the specmd helper so public API content is omitted.
- Treat missing SPEC.md as normal, not an AddDocs failure.
- Update `internal/docubot/SPEC.md` with the behavior.

### Validation
- Run focused package tests and `go test ./...` after implementation.
- Run `check_spec_conformance({"only_changed":true})`.
- Per user request, do not run the full `review` tool in this turn.

## Review

Pending. User requested SPEC conformance but not full review in this turn.

## Summary

Pending.

## State

- Branch: `jn/docubot-spec-context`.
- PR file: `.prs/2026-05-07_1778190850_docubot-spec-context.md`.
- User explicitly does not want `internal/gocodecontext` changed.
- Likely implementation path: `internal/specmd` exposes stripped SPEC body; `internal/docubot` reads `SPEC.md` from package dir and appends/prepends it to AddDocs LLM user context.
