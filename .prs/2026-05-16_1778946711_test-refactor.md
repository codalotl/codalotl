# PR

## User Summary (do not modify)

Read changes in product-spec in this branch. Make it so, number one:
- add go-testing skill, just like spec-md.
    - Just do your best to write some good testing advice.
- add subcommand to refactor command, as described.
    - Again, just do your best for prompt.

Validation:
- in addition to normal tests, you MUST try this manually with something like `go run . exec`. Iterate.

Skip the `review` tool, but do `check_spec_coformance`. Do all steps in orchestrator workflow in one turn.
