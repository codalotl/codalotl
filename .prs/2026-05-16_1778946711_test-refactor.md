# PR

## User Summary (do not modify)

Read changes in product-spec in this branch. Make it so, number one:
- add go-testing skill, just like spec-md.
    - Just do your best to write some good testing advice.
- add subcommand to refactor command, as described.
    - Again, just do your best for prompt.

Validation:
- in addition to normal tests, you MUST try this manually with something like `go run . exec`. Iterate.

Skip the `review` tool, but do `check_spec_coformance`. Do all steps in orchestrator workflow in one turn.

## Plan

### Package internal/skills [DONE]
- Add a built-in `$go-testing` system skill alongside `spec-md` and `skill-creator`.
- Skill guidance should focus on Go test hygiene, maintainability, helpers/table-driven tests, testify when present, interface-boundary testing, and useful Go test/coverage commands.

### Package internal/tools/refactor [DONE]
- Add prompt-style `test-cleanup` refactor using `limited_package_mode` and CAS code-unit caching.
- Add embedded prompt describing the intended test-cleanup scope: improve existing tests, remove/coalesce redundant tests, add helpers/table-driven form when useful, avoid adding missing coverage or radical rewrites.
- Ensure the refactor tool description and tests include `test-cleanup`.

### Validation
- Run focused package tests.
- Run normal tests.
- Manually try the workflow with a command like `go run . exec`.
- Run `check_spec_conformance` with `only_changed=true`.

## Review

Review tool intentionally skipped per user request.

## Summary

## State

- Branch: `jn/test-refactor`.
- Active PR file: `.prs/2026-05-16_1778946711_test-refactor.md`.
- Product specs added on this branch: `product-spec/features/skills.md`, `product-spec/features/refactors/test-cleanup.md`.
- Clarify CAS record existed for `internal/gocas`; required docs-improve refactor was run and reported no opportunity.
- Implemented `$go-testing` in `internal/skills/default/go-testing/SKILL.md`; `go test ./internal/skills` passed.
- Implemented `test-cleanup` refactor in `internal/tools/refactor`; `go test ./internal/tools/refactor` passed.
