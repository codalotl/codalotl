# PR

## User Summary (do not modify)

You just did .prs/2026-07-15_1784123341_add-5.6.md but misunderstood the product.

I tried to clarify in product-spec/features/llms.md

Specifically, gpt-5.6-sol is now a model, but where's the reasoning level? It should be gpt-5.6-sol-high, for instance.

In this PR, fix just the openai models. Use the described reasoning level system. Don't touch opus and whatnot. Make sure we remove 5.4 and 5.5 and 5-mini/5-nano.

## Plan [DONE]

### Package internal/llmmodel [DONE]

- Implement the OpenAI reasoning-level system documented in `internal/llmmodel/SPEC.md`:
  - Register flagship `gpt-5.6-sol` as `-medium`, `-high`, and `-xhigh`.
  - Register non-flagship `gpt-5.6-terra`, `gpt-5.6-luna`, and retained `gpt-5.3-codex` only as `-high`.
  - Do not register reasoning-capable OpenAI models without a reasoning-level suffix.
- Make `gpt-5.6-sol-high` the package and OpenAI provider default.
- Remove `gpt-5.4`, `gpt-5.5`, `gpt-5-mini`, and `gpt-5-nano` from the user-visible built-in model set. Preserve provider metadata as legacy entries for custom-model lookup, as required by the package spec.
- Update focused model registration, default, metadata, and removal tests.
- Update downstream OpenAI default-model examples and expectations where they describe the current default. Preserve historical benchmark data and unrelated provider models.

Implementation landed in `2087544`.

### Package internal/noninteractive/integration [DONE]

- Update `internal/noninteractive/integration/SPEC.md` and current-default expectations in `create_test.go`.
- Preserve replay recordings containing provider-returned versioned model IDs and the isolated test using an arbitrary `gpt-5.4-high` model.

Implementation landed in `8a51ab0`.

### Documentation references [DONE]

- Update `README.md` and `product-spec/features/exec.md` recommendations/examples from `gpt-5.5-high` to `gpt-5.6-sol-high`.
- Preserve historical benchmark references, mock-server example payloads where the model string is arbitrary, replay recordings, and explicit legacy custom-model tests.

Implementation landed in `87be4d7`.

## Review

## Summary

## State

- Branch: `jn/5.6-fix`
- Workspace was clean before planning.
- Relevant package: `internal/llmmodel`
- OpenAI catalog: `internal/llmmodel/config/openai.json`
- Existing GPT-5.6 metadata is present, but models are directly registered without reasoning suffixes and `gpt-5.5-high` remains the default.
- Planning decision: retain `gpt-5.3-codex` because the user explicitly requested removal of four other older model IDs; apply the non-flagship `-high` rule to it.
- SPEC review found the reasoning-variant design coherent and correctly located. It clarified that old provider metadata should remain available for `AddCustomModel` while being removed from default registration.
- Implementation commit `2087544` adds the requested model variants/default, legacy registration, focused coverage, and most downstream default updates.
- `go test ./...` passes after implementation.
- Integration update `8a51ab0` synchronizes current-default examples and generated-case expectations while leaving replay fixtures unchanged.
- `go test ./internal/noninteractive/integration` passes.
- Documentation update `87be4d7` synchronizes the README recommendation and product exec example.
- No non-fixture `gpt-5.5-high` references remain outside PR history. Old model strings used as historical data, mock payloads, replay fixtures, or legacy custom-model coverage are intentionally preserved.
