# PR

## User Summary (do not modify)

read product-spec/features/cas.md

- implement `codalotl cas recertify`

validation:
- tests as normal
- actually try running `go run . cas recertify ...`
    - notice things about it
    - do real QA
    - iterate on ux if it doesn't seem good (but stick to spec)
    - don't commit cas records when QA'ing.

## Plan

### Package internal/q/cas [DONE]
- Extend CAS `AdditionalInfo` with recertification provenance fields so records can say they were copied forward from a previous content hash.

### Package internal/gocas [DONE]
- Add package-level recertification support:
    - no-op when current CAS record exists.
    - locate latest invalidated package record for namespace/hash mode.
    - write new current-hash record by copying prior payload and updating additional info.
    - never delete/mutate existing records.
    - return enough result data/warnings for CLI UX.

### Package internal/cli [DONE]
- Add `codalotl cas recertify <path/to/pkg> --namespaces="<namespace1>[,<namespace2>,...]"`.
- Parse comma-separated registered namespace names; require at least one.
- Print concise per-namespace results and warnings.

### Validation [DONE]
- Run focused tests and normal Go tests.
- QA with `go run . cas recertify ...` using a temporary CAS DB so QA records are not committed.

Validation completed:
- `go test ./...`
- `go test -run TestRun_CAS_Recertify ./internal/cli`
- `go run . spec diff internal/q/cas`
- `go run . spec diff internal/gocas`
- `go run . spec diff internal/cli`
- Manual QA created a temporary module and temporary `CODALOTL_CAS_DB`, preseeded a stale `docs-fix-1` record, ran `go run . cas recertify <tmp-pkg> --namespaces=docs-fix`, verified the copied payload/provenance, then reran and saw `current`.
- Temporary QA records were deleted and were not committed.

## Review

Not run yet per user request.

## Summary

TODO.

## State

- Current branch: `jn/cas-recertify`.
- PR file: `.prs/2026-05-19_1779207425_cas-recertify.md`.
- Product spec: `product-spec/features/cas.md`.
- Likely packages: `internal/q/cas`, `internal/gocas`, `internal/cli`.
- Implementation commit: `354cc62 Implement CAS recertify`.
- Latest workflow state: plan/spec and implementation are done; review and `check_spec_conformance` intentionally not run yet per user request.
