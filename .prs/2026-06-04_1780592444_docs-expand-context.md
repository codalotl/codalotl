# PR

## User Summary (do not modify)

For `codalotl docs add`, i think we have a 10k context right now. This is mostly fine except for a few times when it's not.

First, default to 20k.

Second, if there's a doc batch that's larger than 20k, see if it'll fit in 90% of the context size. If so, do that without error. I think you can query llmmodel to see the model's context size.

Validation:
- in addition to normal,
- actually try to exercise this branch with real tokens with `go run . docs add` against some code (maybe this repo's code). Be sure to validate that the expanded (>20k) tokens are actually used when appropriate.

## Plan

### Package internal/docubot [DONE]
- [DONE] Update `AddDocs` batching so the zero-value/default token budget is 20k.
- [DONE] When the smallest viable documentation batch cannot fit in that default budget, derive the selected model's context window via `llmmodel` and allow that batch if it fits within 90% of the context window.
- [DONE] Keep explicit `AddDocsOptions.TokenBudget` values as caller intent; dynamic expansion is for the default budget path used by `codalotl docs add`.
- [DONE] Add or update tests around default budget behavior, dynamic expansion, and continued budget-exceeded errors when a batch exceeds 90% of model context.
- [DONE] Implement the `internal/docubot/SPEC.md` additions.

### Package internal/cli [DONE]
- [DONE] No implementation change expected: `codalotl docs add` already passes the effective model to `docubot.AddDocs`.
- [DONE] Validate command behavior via existing CLI tests and a real `go run . docs add` exercise that demonstrates an expanded request above 20k tokens.

Validation:
- `go test ./internal/docubot ./internal/cli -run 'TestAddDocs|TestRun_DocsAdd'`
- `go test ./internal/docubot`
- `go test ./internal/cli -run DocsAdd`
- `go run . docs add ./tmp_docs_add_expanded_validation` against a temporary oversized package printed `> Requesting docs for 1 identifiers: Big (28.7k toks)`, confirming the expanded >20k request path. Temporary package removed afterward.

## Review

Review run against `main` after implementation commit `b71d93f`.

Code review verdict: patch is incorrect.

Actionable finding:
- [DONE] [P2] `internal/docubot/add_docs.go`: expanded-budget path pre-check can reject prunable oversized batches. `tokenErr.RequiredTokens` is the unpruned full group cost from the first failed default-budget attempt, so a group whose full cost exceeds 90% context but whose pruned form would fit the expanded budget can incorrectly return `ErrTokenBudgetExceeded`. Fixed in `ab7b659` by retrying `contextForAddDocsPartialWithModule` with the expanded context budget before deciding the batch cannot fit.

SPEC conformance:
- `check_spec_conformance({"only_changed":true})`: `internal/docubot` conforms.

Follow-up validation:
- `go test ./internal/docubot`
- `go test ./internal/gocodecontext`
- `go test ./internal/cli -run DocsAdd`
- Clarify-public-api CAS record created during the follow-up was consumed with `docs-improve-from-clarify` for `internal/gocodecontext`, adding `Context.Prune` documentation about used-by pruning limits.

## Summary

## State

- Active branch: `docs-expand-context`.
- Active PR file: `.prs/2026-06-04_1780592444_docs-expand-context.md`.
- Current locating: default token budget and docs-add request batching live in `internal/docubot/add_docs.go`; CLI passes `effectiveModel(cfg)` to `docubot.AddDocs`.
- Relevant spec edit: `internal/docubot/SPEC.md` says AddDocs defaults to 20k and may expand oversized smallest viable batches up to 90% of selected model context.
- Implementation commit: `b71d93f Expand docs add default context budget`.
- Clarify CAS doc-improvement prerequisite was checked. Refactors for `internal/gocas`, `internal/subscriptions/openaisub`, and `internal/q/cas` were no-ops and made no workspace changes.
- Latest review found one actionable P2 issue in expanded-budget pruning behavior; follow-up implemented in `ab7b659`.
- Latest SPEC conformance passed for `internal/docubot` and wrote a `specconforms-1` CAS record.
- Follow-up implementation commit: `ab7b659 Fix docs add expanded budget pruning`.
