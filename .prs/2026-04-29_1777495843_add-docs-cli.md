# PR

## User Summary (do not modify)

Add to the CLI:

`codalotl docs add path/to/pkg`.

Options:
--public-only
--include-test

Calls docubot's AddDocs function.

## Plan

### Package internal/cli [DONE]
- Add `codalotl docs add <path/to/pkg>`.
- Support `--public-only` and `--include-test`.
- Route command through `docubot.AddDocs`, using normal CLI config for effective model selection and `reflowwidth`.
- Reuse existing single-package path semantics.
- Add focused CLI tests for flag parsing and command wiring.

### Package internal/docubot [DONE]
- No package behavior change planned.
- May reuse patterns from `internal/docubot/cmd`, but keep the user-facing command in `internal/cli`.

## Review

- Not started.
- Implementation for `internal/cli` landed in commit `22da290`.
- Sanity check: `go test ./internal/cli`

## Summary

- Not started.

## State

- Active branch: `jn/add-docs-cli`
- Active PR file: `.prs/2026-04-29_1777495843_add-docs-cli.md`
- Implementation is in `internal/cli/commands.go`
- Tests are in `internal/cli/cli_test.go`
- Reference command wiring exists in `internal/docubot/cmd/main.go`
- Added `runDocubotAddDocs` test seam in `internal/cli`
