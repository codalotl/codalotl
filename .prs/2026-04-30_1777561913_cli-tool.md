# PR

## User Summary (do not modify)

Problem:

- codalotl has many useful CLI commands. Ex: `codalotl context initial`, `codalotl docs add path/to/pkg`, etc.
- in TUI mode, i want to tell the agent to "run codalotl's xyz command"
    - (but this also applies to noninteractive mode)
- But locally, the codalotl binary may not have been installed. We might be in "go run" mode, or in a test.

Solution:

- Make a tool `codalotl_cli`. Params, both required:
    - `subcommand`: if `codalotl` is the command, then the subcommand(s) are the things like `context initial` or `docs add`. Doesn't always have to be 2 words. Can be 1 or 3 or ...
    - `argv`: []string - the arguments to the `codalotl` subcommands. Ex: ['internal/mypkg'] or ['--public-only', 'internal/mypkg'] or ['--search=abcd'] or [] or null.
        - TODO: is this better as args string or argv []string?
        - Instructions for LLM: decide on string vs []string, picking best one.
- I think this tool can live in: `internal/tools/cli` (arguments can be made for various other places)
- So `internal/cli` creates this tool, adds some of its commands to it, and adds it to the registry.
- Add this tool to the generic agent.

This tool itself does NOT know anything about `internal/cli` or which subcommands there are. It is constructed with some `New` type function with a mapping of some kind, or a way to add commands to the resultant tool.

Things I want in the design:
- The tool itself (from the LLM perspective) should be self describing:
    - `codalotl_cli({"subcommand": "help"})` AND `codalotl_cli({"subcommand": "--help"})` should list available commands. `--help` is mentioned in the tool's description.
    - This command also explains that any subcommand can accept `--help` for more details. But --help is never listed as an option in the command's own oneliner or help section.
    - In the actual `codalotl` command, `codalotl --help` only lists root nodes, like `codalotl docs`. But here, it should be expanded, listing only leaves, one per line. 
        - Ex (exact format not prescriptive): `codalotl docs add [--public-api] [--include-test] <pkg>    Add missing documentation comments to a package.`
    - The oneliner:
        - always prepends `codalotl`.
        - then the command
        - then either `[OPTIONS]` if there's many options (if there's none (other than --help): don't incldue it)
        - or [--some-option] [--other=option=<VAL>] if there's 3 or fewer option
        - then non-option args.
            - Use `<pkg>` for a mandatory packge. Use `<something>` for a mandatory something.
            - Use `[<other-thing>]` for an optional other thing.
        - Then a short description.
    - Running `codalotl docs add --help`:
        - Longer description.
        - Prints all options with documentation for each (usually a sentence or sentence fragment)
        - Prints all args iwth documentation for each.
        - Prints examples

Example:

```text
$ codalotl docs add --help
Add missing documentation comments to a package. Never edits comments. Ensures all package-level identifiers have godoc-style doc comments.

Options:
    --include-test  Include test files (by default, test files are not documented).
    --public-only   Only document exported identifiers.

Args:
    <pkg>   path/to/pkg (relative to sandbox dir) or Go import path of package to document.

Examples:
    # Add docs to public/private code in internal/mypkg (excludes test files):
    codalotl docs add internal/mypkg

    # Add docs to public code in internal/mypkg (excludes test files):
    codalotl docs add --public-only internal/mypkg
```

Things I want in the design (cont):
- This should be roughly aligned with `internal/cli` in terms of the help content, args handling, etc. I gave some rather specific things in the design of --help. These don't currently align exactly with cli.
    - So first, align q/cli with this direction (if necessary).
    - Then express existing CLI with that. Right now the CLI help text is horrible.
    - Then build internal/tools/cli
- internal/tools/cli MAY have a dependency on q/cli. It's not necessary, but it is allowed. If it's helpful, we can do that.
- It should be extremely easy to add an existing or new CLI command to codalotl_cli tool. Not all cli commands belong in codalotl_cli! They are whitelisted in.
    - I'm not sure if this is necessary. But i think I want the **option** to customize things like help text in cli vs codalotl_cli. For instance, a llm-oriented help text might have guidance like "ONLY use this if you can't use your module_info tool", which makes no sense for a human.

Ok, more things:
- In order to codalotl_cli-ize a command, the command MUST support a parametric Stdout (io.Writer). The tool will create a io.Writer of some kind, which the command will write to.

UX:

This is a tool call. Display in TUI looks like:

```text
• Running codalotl add docs --public-api internal/lints
  • Need docs for 56 identifiers
  • > Requesting docs for 27 identifiers: goFenceBlock, parsedMarkdown, DiffType, goCodeBlockReflower, *goCodeBlockReflower.close, makeWrappedGoFileSource, gofmtFragment, specDecl, textEdit, applyTextEdits, implSnippetPos, docGroupForDecl, countLeading, validateTripleBacktickFences, parseMarkdown, addEmptyFuncBody, parseDeclFromSnippetBytes, exprString, normalizeDocWhitespace, formatIDs, diffTypeLabel, offsetForPos, specSnippetForDecl, formatDeclNoComments, identListEqual, isGoInfoString, commentGroupRawText (10.2k toks)
  < Got 27 snippets. 27/27 successful
  • > Requesting docs for 16 identifiers: parseGoFileFragmentWithSource, requiredCommentGroupEqual, unwrapWrappedGoFileSource, stripCommentsAndBodies, writeGoFence, idsForDecl, fencedCodeContent, loadImplPackageForSpec, infoStringHasFlag, headingText, findImplForSpecDecl, newGoCodeBlockReflower, snippetStartsWithFuncDecl, parseDeclFragment, parseGoFileFragment, *goCodeBlockReflower.reflow (10.0k toks)
  • Applied 1 documentation change(s):
    - DiffType
• Ran codalotl add docs --public-api internal/lints
```

So that's interesting. Typically this type of display is for subagents, implemented with `internal/agent` and `internal/agentregistry` and all that. But here, these sub-bullets are actually just lines printed by the io.Writer in the CLI code. So we need to support that somehow. I think the main primitive needed is the ability to trigger synthetic agent messages. Then:
- When do we actually do the call to in the tool to trigger the synthetic agent message?
    - On newlines? What if a bunch of output but no newlines?
    - What the CLi code does somthing like this: Fprintf(w, "data:\n- field1: %s\n- field2: %s\n", f1, f2)
        - I think this is better if the User sees this with one bullet/message.
    - So. You come up with specific algo. Roughly:
        - debounce on newline. Flush after, say, 1000ms.
        - flush of course if the cli call returns, even if no newline.
- These bytes need to be sanitized:
    - no tabs
    - allow ansi control? not sure.
    - trim line length. Ex: limit to, say, 400 characters.
    - If the command absolutely SPEWS data (lots of lines, really fast):
        - in the debouncing/flushing code, can elide lines with "... (239 more lines)"
    - what else?
- Generally, the printing of the stuff for the user is a nice-to-have and can be more aggressively sanitized/limited.
- The tool returns the bytes in a more complete manner to the LLM. can still be sanitized a bit (see read file). But don't, for instance, do aggressive "... (239 more lines)" eliding.
- Tool result's presenter doesn't need to re-display the otuput, since we already printed it as synthetic messages.

More things:
- need to support context cancelation of course.

Guidance to orchestrator:
- bro. This is a lot. You're going to have to do this slowly and methodically. Use phases. Plan one phase at a time, leaving later phases as sketches until we get to them. Test and validate as we go.


## Plan

### Phase 0: CLI help foundation [DONE]

This phase makes codalotl's existing command tree describe itself well enough for humans and for the future `codalotl_cli` tool.

#### Review follow-up: q/cli arg fallback

- Feedback: q/cli now omits positional args in help for runnable commands that have `Args` validation but no `Usage`/`ArgHelp` metadata, e.g. `internal/docubot/cmd doc`.
- Decision: fix this in q/cli, not by updating every existing q/cli consumer. The framework should preserve the old generic `[args]` fallback for runnable commands with no richer usage metadata.
- Expected behavior: commands with explicit `Usage`/`ArgHelp` use that richer metadata; otherwise runnable commands show `[args]` in usage/listings. Namespace-only commands still use `<command>`, and runnable commands with children still use `[command]`.
- Implemented in `a5a7593 qcli: preserve generic args usage`.
- Validation: `go test ./internal/q/cli`; `go run . spec diff internal/q/cli`; `go run ./internal/docubot/cmd doc --help`.

#### Review follow-up: no-arg help and root dot alias

- Feedback: the `[args]` fallback now over-advertises args for commands that validate with `qcli.NoArgs`, and root `Usage: "[.]"` combines with subcommands as `codalotl [command] [.]`.
- Decision:
	- Add q/cli help metadata to suppress generic positional arg fallback for known no-positional-arg commands. `ArgsFunc` is opaque, so q/cli should not try to infer this from `Args: qcli.NoArgs`.
	- Treat explicit `Usage` as the complete non-option usage fragment after command path. Generated `[command]`/`<command>` and `[args]` fragments are for commands without explicit `Usage`.
	- In `internal/cli`, mark no-arg commands with the new metadata and make root help use a non-combinatorial usage fragment such as `[command]`, with the `codalotl .` alias described in prose.
- Implemented in `4771025 cli: fix no-arg help and root dot alias` and refined in `e634344 cli: human fixes`.
- Validation: `go test ./internal/q/cli ./internal/cli`; `go run . spec diff internal/q/cli`; `go run . spec diff internal/cli`; `go run . --help`; `go run . config --help`; `go run . version --help`; `go run . context initial --help`; `go run ./internal/docubot/cmd doc --help`.

#### Package `internal/q/cli` [DONE]

- Add command metadata for richer generated help:
	- concise usage fragments for options/args
	- positional arg documentation
	- examples and long descriptions in detailed help
- Keep ordinary CLI help root-oriented by default, but support leaf-command catalog help for tool-style command lists.
- Generated one-line command listings should include full command path, short usage, and short description.
- Built-in `-h`/`--help` remains supported but is omitted from option listings.
- Implemented in `3261680 qcli: add rich help metadata`.
- Validation: `go test ./internal/q/cli`; `go run . spec diff internal/q/cli`.

#### Package `internal/cli` [DONE]

- Populate q/cli command metadata for existing codalotl commands using the command descriptions already in `internal/cli/SPEC.md`.
- Keep `codalotl --help` focused on root commands; command-specific help should become detailed enough for reuse by `codalotl_cli`.
- Commands intended for future tool use must write user-visible stdout through `qcli.Context.Out`.
- Implemented in `b16194c cli: add rich command help`.
- Validation: `go test ./internal/cli`; `go run . spec diff internal/cli`.

### Phase 1: `codalotl_cli` tool package [DONE]

#### Package `internal/tools/cli` [DONE]

- Create a new tool package for `codalotl_cli`.
- Design is in `internal/tools/cli/SPEC.md`; reviewed with no blocking feedback.
- Keep the tool independent of `internal/cli`; construct it with a whitelist of command mappings/runners and help metadata.
- Use params:
	- `subcommand string`
	- `argv []string`
- Treat null `argv` as empty. `argv []string` is preferred over an args string because it preserves exact argument boundaries and avoids shell parsing ambiguity.
- Support `subcommand: "help"` and `subcommand: "--help"` as leaf-command catalog help.
- Support per-command `--help` by rendering detailed command help.
- Execute whitelisted runners with context cancellation and a supplied stdout writer.
- Return complete command output to the LLM, while letting the presenter avoid duplicating output that was already streamed to the user.
- Implemented in `6b7b6f2 tools/cli: add codalotl cli tool`.
- Validation: `gofmt -l internal/tools/cli/cli.go internal/tools/cli/cli_test.go`; `go test ./internal/tools/cli`; `go run . spec diff internal/tools/cli`; `go test ./...`.

### Phase 2: stream CLI output as synthetic agent-visible messages

#### Packages to locate later

- Add a primitive for tool code to emit sanitized, synthetic agent-visible messages while a tool call is running.
- Use this for `codalotl_cli` stdout:
	- debounce flushes on newline
	- flush partial output after about 1s
	- flush when command returns
	- replace tabs/control sequences, bound visible line length, and elide excessive visible output
- Keep the tool result more complete than the visible stream; aggressive elision is for user display only.
- Format visible output as nested bullets under the running tool call in TUI and noninteractive human output.

### Phase 3: wire `codalotl_cli` into agents

#### Packages to locate later

- Add a registry/tool extension path so `internal/cli` can supply `codalotl_cli` to TUI and noninteractive sessions without creating an `internal/cli` <-> `agentbuilder` dependency cycle.
- Let `internal/cli` build the whitelisted tool from its q/cli command definitions/runners and pass it into TUI/noninteractive execution.
- Add `codalotl_cli` to the generic agent only unless later design says package-mode agents also need it.
- Update integration recordings if the intentionally-added generic-agent tool changes request shapes.

## Review

Phase 0 review feedback:

- [DONE] [P2] Preserve arg fallback for commands without usage metadata. `internal/q/cli` should not mislead q/cli consumers that have `Args` validation but no new `Usage`/`ArgHelp` metadata.
- [DONE] [P2] Avoid advertising args for NoArgs commands.
- [DONE] [P3] Don't append the dot alias after subcommands in root codalotl help.

## Summary

Not written.

## State

- Branch: `jn/cli-tool`.
- PR file: `.prs/2026-04-30_1777561913_cli-tool.md`.
- Current step: Phase 1 package implementation is done; next step is planning Phase 2 synthetic output streaming.
- Phase 0 SPEC.md edits drafted and reviewed for `internal/q/cli` and `internal/cli`.
- `internal/q/cli` rich help metadata and reusable `WriteHelp` are implemented.
- `internal/cli` command help metadata is populated; tool-facing stdout paths use `qcli.Context.Out`.
- Phase 0 follow-up validation passed on 2026-04-30: focused q/cli and cli tests, q/cli and cli spec diffs, codalotl root/help checks, and docubot fallback help check.
- `internal/tools/cli` implemented for `codalotl_cli`: in-process q/cli wrapper, fresh whitelisted command-tree factory, `subcommand` + required/nullable `argv`, q/cli help/catalog behavior, separate stdout/stderr result JSON, no agent wiring or synthetic streaming yet.
- `agentbuilder.BuildRegistry()` is currently called inside TUI/noninteractive packages. Wiring `codalotl_cli` from `internal/cli` likely needs an extension hook to avoid a dependency cycle.
