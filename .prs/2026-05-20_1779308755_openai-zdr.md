# PR

## User Summary (do not modify)

In this PR, we enable llmstream to support ZDR (zero data retention) for openai:
- First, kill NoLink. Instead, we rely on NoStore to mean:
    - if the provider doesn't store anything serverside anyway, is this field has no effect.
    - Otherwise, if it does store things server side by default (including linking when appropriate), setting NoStore will enable zdr mode, where we no longer store/link on the server.
- Second, make NoStore work, only for openai responses. If set, we set store=false, and don't link obviously. Each HTTP requset we make will have to include the whole history so that caching actually works.
- Don't set NoStore=true in production code by default. E.g., if I run `go run .` to enter TUI mdoe and use openai, it still uses NoStore=false (just like today).
    - But, you can and should test that NoStore=true actually works against openAI live servers. See below.
    - If $CODALOTL_ZDR=true then `go run .` (and so on) should use NoStore=true.

Implementing this is (probably) the easy-ish part. The hard part is validating it works. You (AIs) have a tendancy to just write code, see if tests pass, and call it a day. That won't work here. You must actually test this, with more than a single trivial request, that the real OpenAI actually works. Moreover, validate that input caching is working properly:
- The env has the real openai keys. Use them. Don't worry about sending live traffic to openai.
- make integration tests (see INTEGRATION_TEST) that exercise this.
- Test this with something like `go run . exec` with $CODALOTL_ZDR=true

## Plan

### Phase 0: design/spec [DONE]

Clarify the NoStore/ZDR semantics in the packages that own provider calls and agent session wiring.

#### Package `internal/llmstream` [DONE]
- Remove `SendOptions.NoLink`.
- For OpenAI Responses, `SendOptions.NoStore` means:
  - request includes `store=false`;
  - no `previous_response_id` is sent;
  - latest response ID is not retained for future linking;
  - every request includes replayable local visible/tool history so multi-turn/tool flows continue to work and prompt caching has the full prefix;
  - provider output item IDs and provider reasoning items requiring stored OpenAI state are not replayed or retained on no-store completed turns.
- Preserve default behavior (`NoStore=false`): store/link via OpenAI response IDs as today.
- Add focused request-shape tests and live `INTEGRATION_TEST=1` coverage that exercises multi-turn/tool ZDR and reports/validates input caching.

#### Package `internal/agent` [DONE]
- Add a construction option for no-store sessions.
- When enabled, every provider send from that agent uses `llmstream.SendOptions{NoStore: true}`.
- Subagents inherit no-store behavior from their parent agent.

#### Package `internal/agentregistry` / prepared agent boundary [DONE]
- Prefer carrying no-store policy via `agent.AgentCreator`, because `PreparedAgent.Create` delegates construction to the supplied creator and only passes registry-resolved options such as model.
- Add agentregistry/toolsetinterface API only if implementation proves the creator path cannot cover the root CLI/TUI entrypoints cleanly.

#### Packages `internal/noninteractive` and `internal/tui` [DONE]
- Default production behavior remains unchanged.
- `CODALOTL_ZDR=true` opts root CLI/TUI sessions into no-store agent construction.
- Noninteractive/tests can verify the env wiring and `go run . exec` can be used for live manual validation.

### Phase 1: implementation

Implement Phase 0 via subagents, likely in this order:
1. [DONE] `internal/llmstream`: remove NoLink, implement OpenAI NoStore request/history semantics, update unit/live integration tests.
2. [DONE] `internal/agent`: add no-store construction option and inheritance.
3. [DONE] Root creation plumbing: prefer passing a no-store-capable `agent.AgentCreator` into prepared agent creation; add agentregistry/toolsetinterface API only if needed.
4. [DONE] `internal/noninteractive` and `internal/tui`: read `CODALOTL_ZDR=true` for root sessions; add tests.

### Phase 2: validation [DONE]

- [DONE] Run targeted package tests after each implementation step.
- [DONE] Run `INTEGRATION_TEST=1 go test ./internal/llmstream -run 'NoStore|ZDR|OpenAI'` with real OpenAI credentials.
- [DONE] Run a live command similar to:
  `CODALOTL_ZDR=true go run . exec --yes --model <openai-model> 'make a small multi-step request that uses tools'`
- [DONE] Inspect diagnostic/live output enough to confirm `store=false`, no linking, multi-turn success, and meaningful cached input on later requests.

## Review

Final review: clean.

- Initial review found that switching from a no-store send back to a stored send could replay provider item IDs from an unstored OpenAI response. Fixed by scrubbing retained no-store assistant turns.
- Follow-up review found completed-success events still exposed unsanitized no-store turns to agents. Fixed by scrubbing no-store completed events before emission.
- Final `review` against `main` reported no findings and overall correctness `patch is correct`.
- Final `check_spec_conformance --only_changed` reported `internal/llmstream` conforms. Earlier changed packages were conforming; CAS conformance records are included.

## Summary

- Implements OpenAI Responses ZDR/no-store semantics through `llmstream.SendOptions.NoStore`: requests use `store=false`, omit `previous_response_id`, do not retain link state, and replay local visible/tool history by value.
- Removes the old `NoLink` API and updates request-shape, replay, and live OpenAI integration coverage, including cached-input validation.
- Scrubs no-store completed turns/events so provider response/item IDs and reasoning state that require stored OpenAI state are not retained or replayed.
- Adds agent no-store construction (`agent.NewOptions.NoStore`), inherited subagent behavior, and default `AgentCreator` option plumbing.
- Wires `CODALOTL_ZDR=true` into noninteractive and TUI root sessions while preserving default production behavior when unset.

Validation:
- `go test -count=1 ./...`
- `INTEGRATION_TEST=1 go test -count=1 -v ./internal/llmstream -run 'NoStore|ZDR|OpenAI'`
- `CODALOTL_ZDR=true LLMSTREAM_LOG_FILE=zdr-live.log go run . exec --yes --no-color --model gpt-5-mini 'Use tools to inspect go.mod and internal/llmstream/open_ai_responses.go. Then answer exactly: ZDR live validation complete.'`
  - Live exec made 3 OpenAI requests; logged requests all had `store=false`, no `previous_response_id`, no input `id` fields, and no reasoning input items.
  - Later turns showed cached input (`3584` then `4480` cached tokens).

## State

- Branch: `jn/openai-zdr`.
- PR file: `.prs/2026-05-20_1779308755_openai-zdr.md`.
- Workspace was clean at start of planning.
- Key current code:
  - `internal/llmstream/llmstream.go`: `SendOptions` has both `NoLink` and `NoStore`; `NoStore` only sets `store=false`.
  - `internal/llmstream/open_ai_responses.go`: `buildOpenAIResponsesParams` trims input and sends `previous_response_id` whenever `sc.providerConversationID` is set; `NoLink` only prevents saving the latest response ID after send.
  - `internal/agent/agent.go`: `sendOnce` currently calls `a.conv.SendAsync(ctx)` with no options.
  - `internal/tui/session.go` and `internal/noninteractive/noninteractive.go`: root sessions call `prepared.Create(agent.NewAgentCreator())`; no ZDR env handling yet.
  - `internal/agentregistry`: `PreparedAgent.Create` delegates construction to the supplied `agent.AgentCreator`; this likely lets entrypoints select no-store without adding registry/tool option fields.
- `internal/llmstream` implementation commit `8ac6e6d`:
  - Removed `SendOptions.NoLink`.
  - OpenAI `NoStore` sends `store=false`, omits `previous_response_id`, resends full local history, and clears retained response-linking state.
  - Default OpenAI send still stores/links and trims local history when linked.
  - Validation passed: `go test -count=1 ./internal/llmstream` and `INTEGRATION_TEST=1 go test -v -run TestOpenAIResponsesProvider_NoStoreZDRToolFlow ./internal/llmstream`.
- `internal/agent` implementation commit `c88da38`:
  - Added `NewOptions.NoStore` and immutable per-agent no-store state.
  - No-store agents pass `llmstream.SendOptions{NoStore: true}` on each provider send.
  - Subagents inherit no-store from parents and can opt in under non-no-store parents.
  - Validation passed: `go test -count=1 ./internal/agent`.
- Root creation plumbing commits `d2c5735` / `abb293c`:
  - `agent.NewAgentCreator(options ...NewOptions)` can carry default construction options into `PreparedAgent.Create`.
  - Creator model defaults remain overrideable per call; no-store defaults are sticky.
  - Validation passed: `go test -count=1 ./internal/agent`.
- Entrypoint wiring commits `67fa556` / `3951673`:
  - `internal/noninteractive` and `internal/tui` pass a no-store root `AgentCreator` only when `CODALOTL_ZDR=true`.
  - Unset and non-exact values preserve default storage behavior.
  - Validation passed: `go test -count=1 ./internal/noninteractive` and `go test -count=1 ./internal/tui`.
- Live validation uncovered and fixed OpenAI integration drift:
  - `749190b`: refreshed live integration models/reasoning efforts.
  - `6350e26`: made the reasoning integration robust when OpenAI does not stream reasoning deltas.
- Live `go run . exec` validation uncovered and fixed a real no-store replay issue:
  - OpenAI rejects replayed persisted output item IDs (`rs_...`, function/custom call item IDs) when prior requests used `store=false`.
  - `187980a`: no-store replay omits provider item IDs and reasoning input items while preserving text, call IDs, tool calls, and tool outputs by value.
- Review uncovered and fixed related ZDR retention/replay issues:
  - `d7626a3`: retained no-store assistant turns scrub provider IDs/reasoning state so later stored-mode full replay cannot include invalid unstored item IDs.
  - `bb65d9b`: no-store completed-success events are scrubbed before agents observe/retain them.
- Phase 2 validation results:
  - `go test -count=1 ./...` passed.
  - `INTEGRATION_TEST=1 go test -count=1 -v ./internal/llmstream -run 'NoStore|ZDR|OpenAI'` passed against live OpenAI.
  - `CODALOTL_ZDR=true LLMSTREAM_LOG_FILE=zdr-live.log go run . exec --yes --no-color --model gpt-5-mini 'Use tools to inspect go.mod and internal/llmstream/open_ai_responses.go. Then answer exactly: ZDR live validation complete.'` passed.
  - The final logged live exec made 3 OpenAI requests; all had `store=false`, no `previous_response_id`, no input `id` fields, no reasoning input items, and later turns showed cached input (`3584` then `4480` cached tokens).
- Final review/spec status:
  - `review` against `main`: no findings, patch correct.
  - `check_spec_conformance --only_changed`: `internal/llmstream` conforms.
