# PR

## User Summary (do not modify)

Background:
- we have the clarify_public_api tool, which answers a question about a package
- Because clarify was used at all, we might strongly suspect the package's public docs didn't naturally answer this question.
- It would be nice if the docs were more useful in the first place, resulting in faster and more reliable use of the package in question.

Solution:
- modify the clarify_public_api tool to (potentially) modify the docs
- the tool works in two phases:
    - the existing subagent works as it does now and does not change (it doesn't get apply_patch, for instance)
    - after that subagent ends, we spin up a new subagent, `improve_public_api_docs`
        - this subagent is only run if the package is inside the sandbox (clarifying a dep from the go.mod file does not edit that dep)
- This new subagent:
    - is jailed to the package
    - has typical package edit tools, but I suppose not change_api and update_usage and things like that
    - is given instructions to only improve docs, but only if it makes sense (like, no need to improve docs if the question is dumb)
        - I want this to be a markdown file in internal/agentbuilder/data
    - is given the question and the answer from the original clarify_public_api subagent
- UX:
    - user sees the clarify_public_api tool call, as they do today
    - user sees the stream of events from the clarify_public_api subagent, as today. The answer itself is hidden, as today
    - user sees more events under the clarify_public_api tool call (same nesting as the above subagent) for the improve subagent. For example, they'll see some Reads and Edits.
    - user sees a final message from the improve subagent; that's fine
    - user sees the clarify_public_api tool result, with the answer
    - In other words, the user mostly sees the same things, but with some extra events.

NOTE:
- The existing clarify_public_api tool is "read only".
- The new one is not. (But the clarify_public_api subagent is still read only).
- The user or caller does not need to know. They don't care. This is not a security or auth problem.
- (you, the orchestrator, have tried and failed this PR before. I dropped all your work. You get tripped up here. If we're clarifying something inside the sandbox, it is allowed to edit it!!)

Use these phases:
- Phase 0: prevalidation: use this phase to just grab a trace of your exec before you change anything. Save the result to this PR file. So you know what it looks like today.
    - Do this even before you plan, so you know what the words I wrote about even mean.
- Phase 1: core impl
- Phase 2: validation
- Phase 3: update internal/noninteractive/integration
    - During phase 1, be sure to tell the implementation agents that you expect this to fail, that its ok.

Validation:
- To validate this, you must try it yourself. Use the noninteractive mode.
    - Eg, something like `go run . exec --package internal/docubot "Examine your usage of the internal/updatedocs package. Formulate a single question you'd like to know the answer to. Use clarify_public_api to get your 1 question answered."`
    - Capture the output. Make sure it makes sense
    - Look at the files in the package you clarified (e.g, internal/updatedocs above). Make sure some docs are added. (or you see evidence that it was a dumb question and the subagent deliberately decided not to update them.)

## Plan

### Phase 0 [DONE]: prevalidation trace

Ran:

`go run . exec --package internal/docubot "Examine your usage of the internal/updatedocs package. Formulate a single question you'd like to know the answer to. Use clarify_public_api to get your 1 question answered."`

Current behavior:
- `clarify_public_api` call appears under the root agent.
- Read-only clarify subagent events appear nested under the tool call.
- Final clarify answer is hidden as a nested final message, then surfaced as the `clarify_public_api` result body.
- No files changed.

Key output excerpt:

```text
• Clarifying API SnippetError in github.com/codalotl/codalotl/internal/updatedocs
  └ When UpdateDocumentation is called with Options{RejectUpdates: true}, can a SnippetError with PartiallyRejected=true represent a snippet where every attempted documentation change was rejected and nothing from that snippet was applied? If so, is there any API-supported way to distinguish 'some docs applied and some rejected' from 'fully rejected due to RejectUpdates'?
  • Reviewing API documentation
  • Read /home/jonathan/projects/codalotl/internal/updatedocs/update_documentation.go
  • Read /home/jonathan/projects/codalotl/internal/updatedocs/readme.txt
  • Read /home/jonathan/projects/codalotl/internal/updatedocs/update_documentation_test.go
  • Investigating documentation updates
  • Read /home/jonathan/projects/codalotl/internal/updatedocs/package_docs.go
  • Read /home/jonathan/projects/codalotl/internal/updatedocs/functions.go
  • Read /home/jonathan/projects/codalotl/internal/updatedocs/types.go
  • Read /home/jonathan/projects/codalotl/internal/updatedocs/values.go
  • Clarifying documentation tests
• Clarified API SnippetError in github.com/codalotl/codalotl/internal/updatedocs
  └ Yes.
    ...
    There is no per-snippet API field that distinguishes those cases.
```

Full run exited 0 in about 1 minute. Git status stayed clean after the run.

### Phase 1 [DONE]: core impl

#### Package `internal/agentbuilder` [DONE]
- Implement `improve_public_api_docs` as a package-mode agent using the `toolset_doc_edit` shape added to `internal/agentbuilder/SPEC.md`.
- Add the agent prompt as a markdown file in `internal/agentbuilder/data`.
- Give the agent package edit/read/test tools, but not mutative package API/usage tools (`change_api`, `update_usage`, etc.).
- The prompt should tell the agent to improve public docs only when the clarification Q&A reveals useful package documentation; leaving files unchanged is acceptable.

#### Package `internal/tools/pkgtools` [DONE]
- Implement the `clarify_public_api` behavior added to `internal/tools/pkgtools/SPEC.md`.
- Keep the existing clarify subagent read-only.
- After collecting the clarify answer, if the resolved package is inside the sandbox, invoke `improve_public_api_docs` jailed to that package with the original question and answer.
- Do not invoke the improver for packages outside the sandbox (go.mod deps, stdlib, or otherwise outside sandbox).
- Return the original clarify answer as the tool result.
- Preserve append-style presentation and nested event flow.
- Phase 1 tests may fail in `internal/noninteractive/integration`; fix recordings in Phase 3.

### Phase 2: validation

- Run package tests for changed packages.
- Manually validate with noninteractive mode using the Phase 0 command or equivalent.
- Inspect the clarified package afterwards for doc edits or clear evidence the improver intentionally left docs unchanged.

### Phase 3: update `internal/noninteractive/integration`

- Update HTTP replay/config expectations for changed event shapes and request payloads.
- `pm-clarify` should likely gain improver subagent events for sandbox packages.
- Dependency/stdlib clarify cases should not show docs-improvement edits.

## Review

Not yet run.

## Summary

Not yet written.

## State

- Active branch: `jn/clarify-edit`.
- PR file: `.prs/2026-05-03_1777822609_clarify-edit.md`.
- Phase 0 completed before planning, per user request.
- Relevant packages:
  - `internal/tools/pkgtools`: owns `clarify_public_api` tool orchestration and presenter.
  - `internal/agentbuilder`: registers built-in agents/tools and YAML-defined agents; data prompts live in `internal/agentbuilder/data`.
  - `internal/noninteractive/integration`: replay tests likely need Phase 3 updates.
- Implemented `internal/agentbuilder` commit:
  - Added YAML-backed `improve_public_api_docs` package-mode agent.
  - Added prompt `internal/agentbuilder/data/improve-public-api-docs.prompt.md`.
  - Toolset: read/list, edit files, skill_shell, diagnostics, fix_lints, run_tests.
  - `go test ./internal/agentbuilder` passed.
- Implemented `internal/tools/pkgtools` commit:
  - Existing clarify subagent remains read-only.
  - Sandbox packages invoke `improve_public_api_docs` after the clarify answer is collected.
  - Outside-sandbox packages skip docs improvement.
  - Clarify result still returns the original answer.
  - Docs-improvement failure is best-effort and does not discard the answer.
  - `go test ./internal/tools/pkgtools` and `go test ./internal/agentbuilder` passed after implementation.
- Spec edits prepared:
  - `internal/agentbuilder/SPEC.md`: adds `improve_public_api_docs` and `toolset_doc_edit`.
  - `internal/tools/pkgtools/SPEC.md`: clarifies read-only answer subagent plus sandbox-only docs improver.
