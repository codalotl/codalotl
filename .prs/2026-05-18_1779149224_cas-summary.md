# PR

## User Summary (do not modify)

read product-spec/features/cas.md - this pr will move closer to this

- delete `codalotl cas set`
- implement `codalotl cas ls-summary`

validation:
- tests as normal
- actually try out `codalotl cas ls-summary` by running `go run . cas ls-summary ...`
    - notice things about it. Does it work? Does churn always show `-`?
    - iterate on ux if it doesn't seem good (but stick to spec)

## Plan

### Package internal/cli [DONE]
- Remove the user-facing `codalotl cas set` command.
- Add `codalotl cas ls-summary <namespace> [--csv]` per `product-spec/features/cas.md`.
- Summary should cover Go packages in the current module/repo, report current CAS hit, invalidated previous CAS presence, age, and churn when derivable.
- Pretty output should be useful in terminals; CSV output should be parseable.
- Validate with package/project tests and by actually running `go run . cas ls-summary ...`.

### Package internal/gocas [DONE]
- Add internal support as needed for enumerating namespace records, matching records to packages, deriving age from CAS metadata/file mtime, and deriving churn from git when possible.
- Keep public API changes minimal; if a public API is added, it should reflect package CAS concepts rather than CLI formatting.

## Review

Full code review not run in this step (user requested stopping after `check_spec_conformance`).

SPEC conformance:
- `check_spec_conformance({"only_changed":true})`
- `internal/cli`: conforms
- `internal/gocas`: conforms

## Summary

TBD.

## State

- PR file: `.prs/2026-05-18_1779149224_cas-summary.md`
- User goal from `product-spec/features/cas.md`: delete `codalotl cas set`; implement `codalotl cas ls-summary <namespace> [--csv]`.
- Current CLI has `cas set`, `cas get`, `cas ls-namespaces`, `cas ls-unset`.
- Existing `internal/cli/cas_ls_unset.go` has useful module package enumeration via `go list`.
- CAS namespace registry is in `internal/cli/cas_command.go`.
- Implementation commit `5f258fa` removes CLI `cas set`, adds `cas ls-summary`, and adds `gocas.SummarizePackage`.
- Validation passed: `go test ./...`, `go run . cas ls-summary specconforms`, `go run . cas ls-summary specconforms --csv`, and `go run . cas --help`.
- Manual output showed churn is not always `-` (`./internal/llmstream`, `./internal/skills`, `./internal/tools/authdomain` showed non-zero churn).
