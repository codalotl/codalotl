# PR

## User Summary (do not modify)

read product-spec/features/pr-orchestrator.md

implement command `codalotl pr new` per spec.

validation:
- normal tests
- actually try running `go run . ...`
    - notice things about it
    - do real QA. Try different things.
    - iterate on ux if it doesn't seem good (but stick to spec)
    - don't commit any generated files when QA'ing.
