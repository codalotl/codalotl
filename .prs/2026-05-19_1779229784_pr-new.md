# PR

## User Summary (do not modify)

read product-spec/features/pr-orchestrator.md

implement command `codalotl pr new` per spec.

validation:
- normal tests
- actually try running `go run . ...`
    - notice things about it
    - do real QA. Try different things.
    - iterate on ux if it doesn't seem good (but stick to spec)
    - don't commit any generated files when QA'ing.

## Plan

### Package internal/cli
- Add `codalotl pr new <feature-name> [--no-git]`, per `product-spec/features/pr-orchestrator.md`.
- Update `internal/cli/SPEC.md` to document command behavior.
- Implement PR file creation under `.prs` with timestamped filename and initial template.
- Implement git setup for normal mode: require clean main/master with upstream in sync, create branch from feature name and optional `$CODALOTL_USER_INITIALS`, commit the PR file, and push to origin when present.
- Implement `--no-git` mode as file creation only, without git requirements.
- Ensure this command does not require LLM configuration/startup validation.
- Validate with focused CLI tests, normal tests, and manual `go run . pr new ...` QA.

## Review

Not run yet.

## Summary

TBD

## State

- Active PR file: `.prs/2026-05-19_1779229784_pr-new.md`
- Product spec: `product-spec/features/pr-orchestrator.md`
- Target package: `internal/cli`
- Branch before implementation: `jn/pr-new`
