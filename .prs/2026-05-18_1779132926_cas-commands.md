# PR

## User Summary (do not modify)

We are going to lay the foundation for `product-spec/features/cas.md`.

- Each package that owns a CAS-record should expose a `gocas.NamespaceSpec`, something in the direction of:

```go
var CASDryNamespace = gocas.NamespaceSpec{
    Name:     "dry",
    Version:  1,
    HashMode: gocas.HashModeCodeUnit,
}
```

- I don't think we need `internal/gocas/casconformance`? If no, delete it (move functionality into owning package).
    - If i'm wrong about that, and it's better to have it, that's fine, keep it. You decide!

- Update gocas public api to accept a `gocas.NamespaceSpec`

- keep filesystem paths of CAS records the same.
- keep internal/q/cas the same. q/cas doesnt care about versions.
- update `codalotl cas get <namespace>` to work with the new system.
- implement `codalotl cas ls-namespaces`

validation:
- in addition to tests,
- use ~ `go run . cas ...` to actually run this stuff
- make a new cas record by using ~ `go run . exec` to run a check_spec_conformance on some package that doesn't have an up-to-date one.
    - commit cas record if its generated.

## Plan

### Phase 0 [DONE] - Product CAS shape

- Updated `product-spec/features/cas.md` to make CLI namespace args non-versioned, keep filesystem namespace dirs versioned, and add `cas ls-namespaces`.

### Phase 1 [DONE] - NamespaceSpec foundation

#### Package `internal/gocas`

- [DONE] Add `NamespaceSpec` and `HashMode`.
- [DONE] Convert `NamespaceSpec{Name, Version}` to existing versioned filesystem namespace strings so paths stay stable.
- [DONE] Replace public package/code-unit methods that accept raw namespace strings with package-oriented methods accepting `NamespaceSpec`.
- [DONE] `HashModePackage` hashes package Go files plus package-local `SPEC.md`.
- [DONE] `HashModeCodeUnit` hashes the package's default Go code unit.

#### CAS-owner packages

- [DONE] Keep `internal/gocas/casconformance`; it remains the owner for spec-conformance metadata and avoids duplicating this domain across CLI/TUI/tool packages.
- [DONE] Expose namespace specs from CAS owners:
    - `internal/gocas/casconformance`: `specconforms`, v1, code-unit hash.
    - `internal/gocas/casclarify`: `clarify-public-api`, v1, package hash.
    - `internal/tools/refactor`: `refactor-*`, v1, code-unit hash.
    - `internal/cli`: `docs-fix`, v1, package hash.
- [DONE] Update owners and callsites to use `gocas.NamespaceSpec`.

#### Package `internal/cli`

- [DONE] Add a CAS namespace registry from the owning packages.
- [DONE] Update `codalotl cas get <namespace>` to accept registered non-versioned namespace names and use the namespace's hash mode.
- [DONE] Update existing `cas set` and `cas ls-unset` to use the same registered namespace resolution.
- [DONE] Add `codalotl cas ls-namespaces`, sorted by namespace name, outputting current name + version and not hash mode.

### Phase 2 - Validation

- [DONE] Run focused package tests for changed packages and `go test ./...`.
- Manually exercise:
    - [DONE] `go run . cas ls-namespaces`
    - `go run . cas get <namespace> <path/to/pkg>`
    - `go run . exec ... check_spec_conformance ...`
- Commit any deliberately generated CAS record.

## Review

Not run yet.

## Summary

TODO

## State

- Branch: `jn/cas-commands`, clean at orchestration start, ahead of `origin/jn/cas-commands`.
- PR file: `.prs/2026-05-18_1779132926_cas-commands.md`.
- Product spec and implementation are updated through Phase 1.
- Validation so far:
    - `go test ./internal/gocas ./internal/gocas/casconformance ./internal/gocas/casclarify ./internal/tools/refactor ./internal/cli`
    - `go test ./...`
    - `go run . cas ls-namespaces` printed `clarify-public-api`, `docs-fix`, `refactor-dry`, `refactor-test-cleanup`, `refactor-test-ensure-coverage`, `specconforms` at version 1.
- A clarify-public-api CAS record was generated during implementation; ran `refactor docs-improve-from-clarify` for `internal/gocas`, which consumed it and improved `Store` documentation.
- Relevant existing packages:
    - `internal/gocas`: Go-aware CAS wrapper; currently raw `gocas.Namespace` APIs split package vs code-unit hash methods.
    - `internal/gocas/casconformance`: spec-conformance CAS owner; currently raw `specconforms-1`.
    - `internal/gocas/casclarify`: clarify-public-api CAS owner; currently raw `clarify-public-api-1`.
    - `internal/tools/refactor`: refactor CAS owner for dry/test cleanup/coverage; currently dynamic raw `refactor-<name>-<generation>`.
    - `internal/cli`: owns docs-fix CAS and user-facing `cas` commands.
